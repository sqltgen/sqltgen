# sqltgen.py — Generated by sqltgen. Do not edit.
from __future__ import annotations
from contextlib import contextmanager
from typing import Any, Generator, Protocol


class _Connection(Protocol):
    def execute(self, sql: str, params: tuple[Any, ...] = ..., /) -> Any: ...


@contextmanager
def execute(conn: _Connection, sql: str, params: tuple[Any, ...] = ()) -> Generator[Any, None, None]:
    """Execute *sql* and yield the cursor."""
    yield conn.execute(sql, params)


def exec_stmt(conn: _Connection, sql: str, params: tuple[Any, ...] = ()) -> None:
    """Execute a statement that returns no result rows."""
    conn.execute(sql, params)
