services:
  db:
    image: ghcr.io/sqltgen/db-postgres:latest
    build:
      context: .
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sqltgen"]
      interval: 0.1s
      timeout: 3s
      retries: 10
