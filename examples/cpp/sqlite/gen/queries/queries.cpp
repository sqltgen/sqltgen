// Generated by sqltgen. Do not edit.
#include "queries.hpp"

static std::string json_escape(const std::string& s) {
    std::string out = "\"";
    for (char c : s) {
        if (c == '\\' || c == '"') out += '\\';
        out += c;
    }
    out += '"';
    return out;
}

void create_author(sqlite3* db, const std::string& name, const std::optional<std::string>& bio, const std::optional<std::int32_t>& birth_year) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_CREATE_AUTHOR.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    sqlite3_bind_text(stmt, 1, name.c_str(), -1, SQLITE_TRANSIENT);
    bio.has_value() ? sqlite3_bind_text(stmt, 2, bio.value().c_str(), -1, SQLITE_TRANSIENT) : sqlite3_bind_null(stmt, 2);
    birth_year.has_value() ? sqlite3_bind_int(stmt, 3, birth_year.value()) : sqlite3_bind_null(stmt, 3);
    int rc = sqlite3_step(stmt);
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
}

std::optional<Author> get_author(sqlite3* db, const std::int32_t& id) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_GET_AUTHOR.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    sqlite3_bind_int(stmt, 1, id);
    std::vector<Author> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(Author{
            sqlite3_column_int(stmt, 0),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 1))),
            sqlite3_column_type(stmt, 2) == SQLITE_NULL ? std::nullopt : std::optional<std::string>(std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2)))),
            sqlite3_column_type(stmt, 3) == SQLITE_NULL ? std::nullopt : std::optional<std::int32_t>(sqlite3_column_int(stmt, 3))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    if (rows.size() > 1) {
        throw std::runtime_error("query returned more than one row");
    }
    return rows.empty() ? std::nullopt : std::optional<Author>(std::move(rows[0]));
}

std::vector<Author> list_authors(sqlite3* db) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_LIST_AUTHORS.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    std::vector<Author> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(Author{
            sqlite3_column_int(stmt, 0),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 1))),
            sqlite3_column_type(stmt, 2) == SQLITE_NULL ? std::nullopt : std::optional<std::string>(std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2)))),
            sqlite3_column_type(stmt, 3) == SQLITE_NULL ? std::nullopt : std::optional<std::int32_t>(sqlite3_column_int(stmt, 3))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    return rows;
}

void create_book(sqlite3* db, const std::int32_t& author_id, const std::string& title, const std::string& genre, const double& price, const std::optional<std::string>& published_at) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_CREATE_BOOK.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    sqlite3_bind_int(stmt, 1, author_id);
    sqlite3_bind_text(stmt, 2, title.c_str(), -1, SQLITE_TRANSIENT);
    sqlite3_bind_text(stmt, 3, genre.c_str(), -1, SQLITE_TRANSIENT);
    sqlite3_bind_double(stmt, 4, price);
    published_at.has_value() ? sqlite3_bind_text(stmt, 5, published_at.value().c_str(), -1, SQLITE_TRANSIENT) : sqlite3_bind_null(stmt, 5);
    int rc = sqlite3_step(stmt);
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
}

std::optional<Book> get_book(sqlite3* db, const std::int32_t& id) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_GET_BOOK.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    sqlite3_bind_int(stmt, 1, id);
    std::vector<Book> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(Book{
            sqlite3_column_int(stmt, 0),
            sqlite3_column_int(stmt, 1),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2))),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 3))),
            sqlite3_column_double(stmt, 4),
            sqlite3_column_type(stmt, 5) == SQLITE_NULL ? std::nullopt : std::optional<std::string>(std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 5))))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    if (rows.size() > 1) {
        throw std::runtime_error("query returned more than one row");
    }
    return rows.empty() ? std::nullopt : std::optional<Book>(std::move(rows[0]));
}

std::vector<Book> get_books_by_ids(sqlite3* db, const std::vector<std::int64_t>& ids) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_GET_BOOKS_BY_IDS.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    std::string ids_json = "[";
    for (size_t i = 0; i < ids.size(); ++i) {
        if (i > 0) ids_json += ",";
        ids_json += std::to_string(ids[i]);
    }
    ids_json += "]";
    sqlite3_bind_text(stmt, 1, ids_json.c_str(), -1, SQLITE_TRANSIENT);
    std::vector<Book> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(Book{
            sqlite3_column_int(stmt, 0),
            sqlite3_column_int(stmt, 1),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2))),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 3))),
            sqlite3_column_double(stmt, 4),
            sqlite3_column_type(stmt, 5) == SQLITE_NULL ? std::nullopt : std::optional<std::string>(std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 5))))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    return rows;
}

std::vector<Book> list_books_by_genre(sqlite3* db, const std::string& genre) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_LIST_BOOKS_BY_GENRE.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    sqlite3_bind_text(stmt, 1, genre.c_str(), -1, SQLITE_TRANSIENT);
    std::vector<Book> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(Book{
            sqlite3_column_int(stmt, 0),
            sqlite3_column_int(stmt, 1),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2))),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 3))),
            sqlite3_column_double(stmt, 4),
            sqlite3_column_type(stmt, 5) == SQLITE_NULL ? std::nullopt : std::optional<std::string>(std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 5))))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    return rows;
}

std::vector<Book> list_books_by_genre_or_all(sqlite3* db, const std::string& genre) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_LIST_BOOKS_BY_GENRE_OR_ALL.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    sqlite3_bind_text(stmt, 1, genre.c_str(), -1, SQLITE_TRANSIENT);
    std::vector<Book> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(Book{
            sqlite3_column_int(stmt, 0),
            sqlite3_column_int(stmt, 1),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2))),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 3))),
            sqlite3_column_double(stmt, 4),
            sqlite3_column_type(stmt, 5) == SQLITE_NULL ? std::nullopt : std::optional<std::string>(std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 5))))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    return rows;
}

void create_customer(sqlite3* db, const std::string& name, const std::string& email) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_CREATE_CUSTOMER.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    sqlite3_bind_text(stmt, 1, name.c_str(), -1, SQLITE_TRANSIENT);
    sqlite3_bind_text(stmt, 2, email.c_str(), -1, SQLITE_TRANSIENT);
    int rc = sqlite3_step(stmt);
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
}

void create_sale(sqlite3* db, const std::int32_t& customer_id) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_CREATE_SALE.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    sqlite3_bind_int(stmt, 1, customer_id);
    int rc = sqlite3_step(stmt);
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
}

void add_sale_item(sqlite3* db, const std::int32_t& sale_id, const std::int32_t& book_id, const std::int32_t& quantity, const double& unit_price) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_ADD_SALE_ITEM.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    sqlite3_bind_int(stmt, 1, sale_id);
    sqlite3_bind_int(stmt, 2, book_id);
    sqlite3_bind_int(stmt, 3, quantity);
    sqlite3_bind_double(stmt, 4, unit_price);
    int rc = sqlite3_step(stmt);
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
}

std::vector<ListBooksWithAuthorRow> list_books_with_author(sqlite3* db) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_LIST_BOOKS_WITH_AUTHOR.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    std::vector<ListBooksWithAuthorRow> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(ListBooksWithAuthorRow{
            sqlite3_column_int(stmt, 0),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 1))),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2))),
            sqlite3_column_double(stmt, 3),
            sqlite3_column_type(stmt, 4) == SQLITE_NULL ? std::nullopt : std::optional<std::string>(std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 4)))),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 5))),
            sqlite3_column_type(stmt, 6) == SQLITE_NULL ? std::nullopt : std::optional<std::string>(std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 6))))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    return rows;
}

std::vector<Book> get_books_never_ordered(sqlite3* db) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_GET_BOOKS_NEVER_ORDERED.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    std::vector<Book> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(Book{
            sqlite3_column_int(stmt, 0),
            sqlite3_column_int(stmt, 1),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2))),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 3))),
            sqlite3_column_double(stmt, 4),
            sqlite3_column_type(stmt, 5) == SQLITE_NULL ? std::nullopt : std::optional<std::string>(std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 5))))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    return rows;
}

std::vector<GetTopSellingBooksRow> get_top_selling_books(sqlite3* db) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_GET_TOP_SELLING_BOOKS.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    std::vector<GetTopSellingBooksRow> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(GetTopSellingBooksRow{
            sqlite3_column_int(stmt, 0),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 1))),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2))),
            sqlite3_column_double(stmt, 3),
            sqlite3_column_type(stmt, 4) == SQLITE_NULL ? std::nullopt : std::optional<std::int64_t>(sqlite3_column_int64(stmt, 4))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    return rows;
}

std::vector<GetBestCustomersRow> get_best_customers(sqlite3* db) {
    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db, SQL_GET_BEST_CUSTOMERS.c_str(), -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    std::vector<GetBestCustomersRow> rows;
    int rc;
    while ((rc = sqlite3_step(stmt)) == SQLITE_ROW) {
        rows.push_back(GetBestCustomersRow{
            sqlite3_column_int(stmt, 0),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 1))),
            std::string(reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2))),
            sqlite3_column_type(stmt, 3) == SQLITE_NULL ? std::nullopt : std::optional<double>(sqlite3_column_double(stmt, 3))
        });
    }
    sqlite3_finalize(stmt);
    if (rc != SQLITE_DONE) {
        throw std::runtime_error(sqlite3_errmsg(db));
    }
    return rows;
}


void Querier::create_author(const std::string& name, const std::optional<std::string>& bio, const std::optional<std::int32_t>& birth_year) {
    return ::create_author(db_, name, bio, birth_year);
}

std::optional<Author> Querier::get_author(const std::int32_t& id) {
    return ::get_author(db_, id);
}

std::vector<Author> Querier::list_authors() {
    return ::list_authors(db_);
}

void Querier::create_book(const std::int32_t& author_id, const std::string& title, const std::string& genre, const double& price, const std::optional<std::string>& published_at) {
    return ::create_book(db_, author_id, title, genre, price, published_at);
}

std::optional<Book> Querier::get_book(const std::int32_t& id) {
    return ::get_book(db_, id);
}

std::vector<Book> Querier::get_books_by_ids(const std::vector<std::int64_t>& ids) {
    return ::get_books_by_ids(db_, ids);
}

std::vector<Book> Querier::list_books_by_genre(const std::string& genre) {
    return ::list_books_by_genre(db_, genre);
}

std::vector<Book> Querier::list_books_by_genre_or_all(const std::string& genre) {
    return ::list_books_by_genre_or_all(db_, genre);
}

void Querier::create_customer(const std::string& name, const std::string& email) {
    return ::create_customer(db_, name, email);
}

void Querier::create_sale(const std::int32_t& customer_id) {
    return ::create_sale(db_, customer_id);
}

void Querier::add_sale_item(const std::int32_t& sale_id, const std::int32_t& book_id, const std::int32_t& quantity, const double& unit_price) {
    return ::add_sale_item(db_, sale_id, book_id, quantity, unit_price);
}

std::vector<ListBooksWithAuthorRow> Querier::list_books_with_author() {
    return ::list_books_with_author(db_);
}

std::vector<Book> Querier::get_books_never_ordered() {
    return ::get_books_never_ordered(db_);
}

std::vector<GetTopSellingBooksRow> Querier::get_top_selling_books() {
    return ::get_top_selling_books(db_);
}

std::vector<GetBestCustomersRow> Querier::get_best_customers() {
    return ::get_best_customers(db_);
}
