// Generated by sqltgen. Do not edit.
// Runtime: pg (node-postgres) — npm install pg
import type { ConnectFn, Db } from '../sqltgen';
import { releaseDb } from '../sqltgen';

import type { Task } from '../models/task';

import type { Priority } from '../models/priority';
import type { Status } from '../models/status';

const SQL_CREATE_TASK = `INSERT INTO task (title, priority, status, description)
VALUES ($1, $2, $3, $4)
RETURNING *`;
const SQL_GET_TASK = `SELECT id, title, priority, status, description
FROM task
WHERE id = $1`;
const SQL_LIST_TASKS_BY_PRIORITY = `SELECT id, title, priority, status
FROM task
WHERE priority = $1
ORDER BY id`;
const SQL_LIST_TASKS_BY_STATUS = `SELECT id, title, priority, status
FROM task
WHERE status = $1
ORDER BY id`;
const SQL_UPDATE_TASK_STATUS = `UPDATE task SET status = $1 WHERE id = $2
RETURNING *`;
const SQL_LIST_TASKS_BY_PRIORITY_OR_ALL = `SELECT id, title, priority, status
FROM task
WHERE ($1::priority IS NULL OR priority = $1::priority)
ORDER BY id`;
const SQL_COUNT_BY_STATUS = `SELECT status, COUNT(*) AS task_count
FROM task
GROUP BY status
ORDER BY status`;
const SQL_CREATE_TASK_WITH_TAGS = `INSERT INTO task (title, priority, status, description, tags)
VALUES ($1, $2, $3, $4, $5)
RETURNING *`;
const SQL_GET_TASK_TAGS = `SELECT id, title, tags
FROM task
WHERE id = $1`;
const SQL_UPDATE_TASK_TAGS = `UPDATE task SET tags = $1 WHERE id = $2
RETURNING *`;
const SQL_DELETE_TASK = `DELETE FROM task WHERE id = $1`;

export async function createTask(db: Db, title: string, priority: Priority, status: Status, description: string | null): Promise<Task | null> {
  const result = await db.query<Task>(SQL_CREATE_TASK, [title, priority, status, description]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

export interface GetTaskRow {
  id: bigint;
  title: string;
  priority: Priority;
  status: Status;
  description: string | null;
}

export async function getTask(db: Db, id: bigint): Promise<GetTaskRow | null> {
  const result = await db.query<GetTaskRow>(SQL_GET_TASK, [String(id)]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id) };
}

export interface ListTasksByPriorityRow {
  id: bigint;
  title: string;
  priority: Priority;
  status: Status;
}

export async function listTasksByPriority(db: Db, priority: Priority): Promise<ListTasksByPriorityRow[]> {
  const result = await db.query<ListTasksByPriorityRow>(SQL_LIST_TASKS_BY_PRIORITY, [priority]);
  const rows = result.rows;
  return rows.map(raw => ({ ...raw, id: BigInt(raw.id) }));
}

export interface ListTasksByStatusRow {
  id: bigint;
  title: string;
  priority: Priority;
  status: Status;
}

export async function listTasksByStatus(db: Db, status: Status): Promise<ListTasksByStatusRow[]> {
  const result = await db.query<ListTasksByStatusRow>(SQL_LIST_TASKS_BY_STATUS, [status]);
  const rows = result.rows;
  return rows.map(raw => ({ ...raw, id: BigInt(raw.id) }));
}

export async function updateTaskStatus(db: Db, status: Status, id: bigint): Promise<Task | null> {
  const result = await db.query<Task>(SQL_UPDATE_TASK_STATUS, [status, String(id)]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

export interface ListTasksByPriorityOrAllRow {
  id: bigint;
  title: string;
  priority: Priority;
  status: Status;
}

export async function listTasksByPriorityOrAll(db: Db, priority: Priority | null): Promise<ListTasksByPriorityOrAllRow[]> {
  const result = await db.query<ListTasksByPriorityOrAllRow>(SQL_LIST_TASKS_BY_PRIORITY_OR_ALL, [priority]);
  const rows = result.rows;
  return rows.map(raw => ({ ...raw, id: BigInt(raw.id) }));
}

export interface CountByStatusRow {
  status: Status;
  task_count: bigint;
}

export async function countByStatus(db: Db): Promise<CountByStatusRow[]> {
  const result = await db.query<CountByStatusRow>(SQL_COUNT_BY_STATUS, []);
  const rows = result.rows;
  return rows.map(raw => ({ ...raw, task_count: BigInt(raw.task_count) }));
}

export async function createTaskWithTags(db: Db, title: string, priority: Priority, status: Status, description: string | null, tags: Priority[]): Promise<Task | null> {
  const result = await db.query<Task>(SQL_CREATE_TASK_WITH_TAGS, [title, priority, status, description, tags]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

export interface GetTaskTagsRow {
  id: bigint;
  title: string;
  tags: Priority[];
}

export async function getTaskTags(db: Db, id: bigint): Promise<GetTaskTagsRow | null> {
  const result = await db.query<GetTaskTagsRow>(SQL_GET_TASK_TAGS, [String(id)]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

export async function updateTaskTags(db: Db, tags: Priority[], id: bigint): Promise<Task | null> {
  const result = await db.query<Task>(SQL_UPDATE_TASK_TAGS, [tags, String(id)]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

export async function deleteTask(db: Db, id: bigint): Promise<void> {
  await db.query(SQL_DELETE_TASK, [String(id)]);
}

export class Querier {
  constructor(private readonly connect: ConnectFn) {}

  async createTask(title: string, priority: Priority, status: Status, description: string | null): Promise<Task | null> {
    const db = await this.connect();
    try {
      return createTask(db, title, priority, status, description);
    } finally {
      await releaseDb(db);
    }
  }

  async getTask(id: bigint): Promise<GetTaskRow | null> {
    const db = await this.connect();
    try {
      return getTask(db, id);
    } finally {
      await releaseDb(db);
    }
  }

  async listTasksByPriority(priority: Priority): Promise<ListTasksByPriorityRow[]> {
    const db = await this.connect();
    try {
      return listTasksByPriority(db, priority);
    } finally {
      await releaseDb(db);
    }
  }

  async listTasksByStatus(status: Status): Promise<ListTasksByStatusRow[]> {
    const db = await this.connect();
    try {
      return listTasksByStatus(db, status);
    } finally {
      await releaseDb(db);
    }
  }

  async updateTaskStatus(status: Status, id: bigint): Promise<Task | null> {
    const db = await this.connect();
    try {
      return updateTaskStatus(db, status, id);
    } finally {
      await releaseDb(db);
    }
  }

  async listTasksByPriorityOrAll(priority: Priority | null): Promise<ListTasksByPriorityOrAllRow[]> {
    const db = await this.connect();
    try {
      return listTasksByPriorityOrAll(db, priority);
    } finally {
      await releaseDb(db);
    }
  }

  async countByStatus(): Promise<CountByStatusRow[]> {
    const db = await this.connect();
    try {
      return countByStatus(db);
    } finally {
      await releaseDb(db);
    }
  }

  async createTaskWithTags(title: string, priority: Priority, status: Status, description: string | null, tags: Priority[]): Promise<Task | null> {
    const db = await this.connect();
    try {
      return createTaskWithTags(db, title, priority, status, description, tags);
    } finally {
      await releaseDb(db);
    }
  }

  async getTaskTags(id: bigint): Promise<GetTaskTagsRow | null> {
    const db = await this.connect();
    try {
      return getTaskTags(db, id);
    } finally {
      await releaseDb(db);
    }
  }

  async updateTaskTags(tags: Priority[], id: bigint): Promise<Task | null> {
    const db = await this.connect();
    try {
      return updateTaskTags(db, tags, id);
    } finally {
      await releaseDb(db);
    }
  }

  async deleteTask(id: bigint): Promise<void> {
    const db = await this.connect();
    try {
      return deleteTask(db, id);
    } finally {
      await releaseDb(db);
    }
  }
}
