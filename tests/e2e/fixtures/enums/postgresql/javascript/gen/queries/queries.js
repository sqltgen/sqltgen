// Generated by sqltgen. Do not edit.
// Runtime: pg (node-postgres) — npm install pg
import { releaseDb } from '../sqltgen.js';
/** @typedef {import('../sqltgen.js').Db} Db */
/** @typedef {import('../sqltgen.js').ConnectFn} ConnectFn */

/** @typedef {import('../models/task.js').Task} Task */

/** @typedef {import('../models/priority.js').Priority} Priority */
/** @typedef {import('../models/status.js').Status} Status */

const SQL_CREATE_TASK = `INSERT INTO task (title, priority, status, description)
VALUES ($1, $2, $3, $4)
RETURNING *`;
const SQL_GET_TASK = `SELECT id, title, priority, status, description
FROM task
WHERE id = $1`;
const SQL_LIST_TASKS_BY_PRIORITY = `SELECT id, title, priority, status
FROM task
WHERE priority = $1
ORDER BY id`;
const SQL_LIST_TASKS_BY_STATUS = `SELECT id, title, priority, status
FROM task
WHERE status = $1
ORDER BY id`;
const SQL_UPDATE_TASK_STATUS = `UPDATE task SET status = $1 WHERE id = $2
RETURNING *`;
const SQL_LIST_TASKS_BY_PRIORITY_OR_ALL = `SELECT id, title, priority, status
FROM task
WHERE ($1::priority IS NULL OR priority = $1::priority)
ORDER BY id`;
const SQL_COUNT_BY_STATUS = `SELECT status, COUNT(*) AS task_count
FROM task
GROUP BY status
ORDER BY status`;
const SQL_CREATE_TASK_WITH_TAGS = `INSERT INTO task (title, priority, status, description, tags)
VALUES ($1, $2, $3, $4, $5)
RETURNING *`;
const SQL_GET_TASK_TAGS = `SELECT id, title, tags
FROM task
WHERE id = $1`;
const SQL_UPDATE_TASK_TAGS = `UPDATE task SET tags = $1 WHERE id = $2
RETURNING *`;
const SQL_DELETE_TASK = `DELETE FROM task WHERE id = $1`;

/**
 * @param {Db} db
 * @param {string} title
 * @param {Priority} priority
 * @param {Status} status
 * @param {string | null} description
 * @returns {Promise<Task | null>}
 */
export async function createTask(db, title, priority, status, description) {
  const result = await db.query(SQL_CREATE_TASK, [title, priority, status, description]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

/**
 * @typedef {Object} GetTaskRow
 * @property {bigint} id
 * @property {string} title
 * @property {Priority} priority
 * @property {Status} status
 * @property {string | null} description
 */

/**
 * @param {Db} db
 * @param {bigint} id
 * @returns {Promise<GetTaskRow | null>}
 */
export async function getTask(db, id) {
  const result = await db.query(SQL_GET_TASK, [String(id)]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id) };
}

/**
 * @typedef {Object} ListTasksByPriorityRow
 * @property {bigint} id
 * @property {string} title
 * @property {Priority} priority
 * @property {Status} status
 */

/**
 * @param {Db} db
 * @param {Priority} priority
 * @returns {Promise<ListTasksByPriorityRow[]>}
 */
export async function listTasksByPriority(db, priority) {
  const result = await db.query(SQL_LIST_TASKS_BY_PRIORITY, [priority]);
  const rows = result.rows;
  return rows.map(raw => ({ ...raw, id: BigInt(raw.id) }));
}

/**
 * @typedef {Object} ListTasksByStatusRow
 * @property {bigint} id
 * @property {string} title
 * @property {Priority} priority
 * @property {Status} status
 */

/**
 * @param {Db} db
 * @param {Status} status
 * @returns {Promise<ListTasksByStatusRow[]>}
 */
export async function listTasksByStatus(db, status) {
  const result = await db.query(SQL_LIST_TASKS_BY_STATUS, [status]);
  const rows = result.rows;
  return rows.map(raw => ({ ...raw, id: BigInt(raw.id) }));
}

/**
 * @param {Db} db
 * @param {Status} status
 * @param {bigint} id
 * @returns {Promise<Task | null>}
 */
export async function updateTaskStatus(db, status, id) {
  const result = await db.query(SQL_UPDATE_TASK_STATUS, [status, String(id)]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

/**
 * @typedef {Object} ListTasksByPriorityOrAllRow
 * @property {bigint} id
 * @property {string} title
 * @property {Priority} priority
 * @property {Status} status
 */

/**
 * @param {Db} db
 * @param {Priority | null} priority
 * @returns {Promise<ListTasksByPriorityOrAllRow[]>}
 */
export async function listTasksByPriorityOrAll(db, priority) {
  const result = await db.query(SQL_LIST_TASKS_BY_PRIORITY_OR_ALL, [priority]);
  const rows = result.rows;
  return rows.map(raw => ({ ...raw, id: BigInt(raw.id) }));
}

/**
 * @typedef {Object} CountByStatusRow
 * @property {Status} status
 * @property {bigint} task_count
 */

/**
 * @param {Db} db
 * @returns {Promise<CountByStatusRow[]>}
 */
export async function countByStatus(db) {
  const result = await db.query(SQL_COUNT_BY_STATUS, []);
  const rows = result.rows;
  return rows.map(raw => ({ ...raw, task_count: BigInt(raw.task_count) }));
}

/**
 * @param {Db} db
 * @param {string} title
 * @param {Priority} priority
 * @param {Status} status
 * @param {string | null} description
 * @param {Priority[]} tags
 * @returns {Promise<Task | null>}
 */
export async function createTaskWithTags(db, title, priority, status, description, tags) {
  const result = await db.query(SQL_CREATE_TASK_WITH_TAGS, [title, priority, status, description, tags]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

/**
 * @typedef {Object} GetTaskTagsRow
 * @property {bigint} id
 * @property {string} title
 * @property {Priority[]} tags
 */

/**
 * @param {Db} db
 * @param {bigint} id
 * @returns {Promise<GetTaskTagsRow | null>}
 */
export async function getTaskTags(db, id) {
  const result = await db.query(SQL_GET_TASK_TAGS, [String(id)]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

/**
 * @param {Db} db
 * @param {Priority[]} tags
 * @param {bigint} id
 * @returns {Promise<Task | null>}
 */
export async function updateTaskTags(db, tags, id) {
  const result = await db.query(SQL_UPDATE_TASK_TAGS, [tags, String(id)]);
  const raw = result.rows[0];
  if (!raw) return null;
  return { ...raw, id: BigInt(raw.id), tags: typeof raw.tags === 'string' ? raw.tags.replace(/[{}]/g, '').split(',').filter(Boolean) : (raw.tags ?? []) };
}

/**
 * @param {Db} db
 * @param {bigint} id
 * @returns {Promise<void>}
 */
export async function deleteTask(db, id) {
  await db.query(SQL_DELETE_TASK, [String(id)]);
}

export class Querier {
  /** @param {ConnectFn} connect */
  constructor(connect) {
    this.connect = connect;
  }

  async createTask(title, priority, status, description) {
    const db = await this.connect();
    try {
      return createTask(db, title, priority, status, description);
    } finally {
      await releaseDb(db);
    }
  }

  async getTask(id) {
    const db = await this.connect();
    try {
      return getTask(db, id);
    } finally {
      await releaseDb(db);
    }
  }

  async listTasksByPriority(priority) {
    const db = await this.connect();
    try {
      return listTasksByPriority(db, priority);
    } finally {
      await releaseDb(db);
    }
  }

  async listTasksByStatus(status) {
    const db = await this.connect();
    try {
      return listTasksByStatus(db, status);
    } finally {
      await releaseDb(db);
    }
  }

  async updateTaskStatus(status, id) {
    const db = await this.connect();
    try {
      return updateTaskStatus(db, status, id);
    } finally {
      await releaseDb(db);
    }
  }

  async listTasksByPriorityOrAll(priority) {
    const db = await this.connect();
    try {
      return listTasksByPriorityOrAll(db, priority);
    } finally {
      await releaseDb(db);
    }
  }

  async countByStatus() {
    const db = await this.connect();
    try {
      return countByStatus(db);
    } finally {
      await releaseDb(db);
    }
  }

  async createTaskWithTags(title, priority, status, description, tags) {
    const db = await this.connect();
    try {
      return createTaskWithTags(db, title, priority, status, description, tags);
    } finally {
      await releaseDb(db);
    }
  }

  async getTaskTags(id) {
    const db = await this.connect();
    try {
      return getTaskTags(db, id);
    } finally {
      await releaseDb(db);
    }
  }

  async updateTaskTags(tags, id) {
    const db = await this.connect();
    try {
      return updateTaskTags(db, tags, id);
    } finally {
      await releaseDb(db);
    }
  }

  async deleteTask(id) {
    const db = await this.connect();
    try {
      return deleteTask(db, id);
    } finally {
      await releaseDb(db);
    }
  }
}
