// Code generated by sqltgen. Do not edit.
package gen

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5"
)

const SQL_INSERT_RECORD = `
INSERT INTO record (label, timestamps, uuids)
VALUES ($1, $2, $3)
`
const SQL_GET_RECORD = `
SELECT id, label, timestamps, uuids
FROM record
WHERE id = $1
`

// InsertRecord executes the InsertRecord query.
func InsertRecord(ctx context.Context, db DBTX, label string, timestamps []time.Time, uuids []string) error {
	_, err := db.Exec(ctx, SQL_INSERT_RECORD, label, timestamps, uuids)
	return err
}

// GetRecord executes the GetRecord query.
func GetRecord(ctx context.Context, db DBTX, id int64) (*Record, error) {
	row := db.QueryRow(ctx, SQL_GET_RECORD, id)
	var r Record
	err := row.Scan(&r.Id, &r.Label, &r.Timestamps, &r.Uuids)
	if err == pgx.ErrNoRows {
		return nil, nil
	}
	if err != nil {
		return nil, err
	}
	return &r, nil
}

// Querier wraps a DBTX and exposes named query methods.
type Querier struct {
	db DBTX
}

// NewQuerier returns a new Querier backed by db.
func NewQuerier(db DBTX) *Querier {
	return &Querier{db: db}
}

// InsertRecord delegates to the package-level InsertRecord function.
func (q *Querier) InsertRecord(ctx context.Context, label string, timestamps []time.Time, uuids []string) error {
	return InsertRecord(ctx, q.db, label, timestamps, uuids)
}

// GetRecord delegates to the package-level GetRecord function.
func (q *Querier) GetRecord(ctx context.Context, id int64) (*Record, error) {
	return GetRecord(ctx, q.db, id)
}
