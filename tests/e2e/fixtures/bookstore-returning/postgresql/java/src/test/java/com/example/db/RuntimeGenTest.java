// Generated e2e runtime test — java/postgresql. DO NOT EDIT.
//
// Auto-generated by scripts/e2e_testgen from test_spec.yaml.
package com.example.db;

import static org.junit.jupiter.api.Assertions.*;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.Statement;
import java.time.*;
import java.util.List;
import java.util.UUID;
import com.example.db.queries.Queries;
import com.example.db.models.*;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

// ─── test setup ───────────────────────────────────────────────────────────────

class RuntimeGenTest {

    private static final ObjectMapper GEN_MAPPER = new ObjectMapper();
    private static final String URL =
        System.getenv().getOrDefault("DATABASE_URL",
            "jdbc:postgresql://localhost:15432/sqltgen_e2e");
    private static final String USER = "sqltgen";
    private static final String PASS = "sqltgen";

    private Connection conn;
    private String dbName;

    @BeforeEach
    void setUp() throws Exception {
        dbName = "test_" + UUID.randomUUID().toString().replace("-", "");
        try (Connection admin = DriverManager.getConnection(URL, USER, PASS);
             Statement s = admin.createStatement()) {
            s.execute("CREATE DATABASE \"" + dbName + "\"");
        }
        String baseUrl = URL.replaceFirst("/[^/]*$", "/" + dbName);
        conn = DriverManager.getConnection(baseUrl, USER, PASS);
        conn.setAutoCommit(true);
        var ddl = java.nio.file.Files.readString(
            java.nio.file.Path.of("../schema.sql"));
        try (Statement s = conn.createStatement()) {
            for (String stmt : ddl.split(";")) {
                String t = stmt.strip();
                if (!t.isEmpty()) s.execute(t);
            }
        }
    }

    @AfterEach
    void tearDown() throws Exception {
        if (conn != null) conn.close();
        try (Connection admin = DriverManager.getConnection(URL, USER, PASS);
             Statement s = admin.createStatement()) {
            s.execute("DROP DATABASE IF EXISTS \"" + dbName + "\"");
        }
    }

    private static JsonNode genJson(String raw) {
        try {
            return GEN_MAPPER.readTree(raw);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }

// ─── scenarios ────────────────────────────────────────────────────────────────

    // ─── DELETE RETURNING ────────────────────────────────────────────────────────────

    @Test
    void testDeleteAuthorReturnsRowGen() throws Exception {
        Queries.createAuthor(conn, "ToDelete", null, 1900);
        var deleted = Queries.deleteAuthor(conn, 1L).orElseThrow();
        assertNotNull(deleted);
        assertEquals("ToDelete", deleted.name());
        var gone = Queries.getAuthor(conn, 1L);
        assertTrue(gone.isEmpty());
    }

    @Test
    void testDeleteAuthorNotFoundGen() throws Exception {
        var result = Queries.deleteAuthor(conn, 999L);
        assertTrue(result.isEmpty());
    }

    // ─── INSERT RETURNING ────────────────────────────────────────────────────────────

    @Test
    void testCreateAuthorReturnsRowGen() throws Exception {
        var author = Queries.createAuthor(conn, "Asimov", "Sci-fi master", 1920).orElseThrow();
        assertNotNull(author);
        assertEquals("Asimov", author.name());
        assertEquals("Sci-fi master", author.bio());
        assertEquals(1920, author.birthYear());
    }

    @Test
    void testCreateAuthorNullBioGen() throws Exception {
        var author = Queries.createAuthor(conn, "Herbert", null, 1920).orElseThrow();
        assertNotNull(author);
        assertEquals("Herbert", author.name());
        assertNull(author.bio());
    }

    @Test
    void testCreateBookReturnsRowGen() throws Exception {
        Queries.createAuthor(conn, "Author", null, 1900);
        var book = Queries.createBook(conn, 1L, "Dune", "sci-fi", new java.math.BigDecimal("12.99"), LocalDate.of(1965, 6, 1)).orElseThrow();
        assertNotNull(book);
        assertEquals("Dune", book.title());
        assertEquals("sci-fi", book.genre());
    }

    // ─── UPDATE RETURNING ────────────────────────────────────────────────────────────

    @Test
    void testUpdateAuthorBioReturnsRowGen() throws Exception {
        Queries.createAuthor(conn, "Author", "Old bio", 1900);
        var updated = Queries.updateAuthorBio(conn, "New bio", 1L).orElseThrow();
        assertNotNull(updated);
        assertEquals("Author", updated.name());
        assertEquals("New bio", updated.bio());
    }

    @Test
    void testUpdateAuthorBioToNullGen() throws Exception {
        Queries.createAuthor(conn, "Author", "Has bio", 1900);
        var updated = Queries.updateAuthorBio(conn, null, 1L).orElseThrow();
        assertNotNull(updated);
        assertNull(updated.bio());
    }

}
