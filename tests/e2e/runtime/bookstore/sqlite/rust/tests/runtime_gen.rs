// Generated e2e runtime test — rust/sqlite. DO NOT EDIT.
//
// Auto-generated by scripts/e2e_testgen from test_spec.yaml.
use std::str::FromStr;
use e2e_bookstore_rust_sqlite::db::queries::queries;
use sqlx::SqlitePool;
#[allow(unused_imports)]
use time::macros::{date, datetime, time};

// ─── test setup ───────────────────────────────────────────────────────────────

async fn gen_setup_db() -> SqlitePool {
    let pool = SqlitePool::connect("sqlite::memory:").await.unwrap();
    let ddl = include_str!("../../../../../fixtures/bookstore/sqlite/schema.sql");
    for stmt in ddl.split(';').map(str::trim).filter(|s| !s.is_empty()) {
        sqlx::query(stmt).execute(&pool).await.unwrap();
    }
    pool
}

#[allow(dead_code)]
fn gen_assert_json_str(got: &str, want: serde_json::Value) {
    let got: serde_json::Value = serde_json::from_str(got).unwrap();
    assert_eq!(got, want);
}

// ─── scenarios ────────────────────────────────────────────────────────────────

// ─── :exec queries ────────────────────────────────────────────────────────────

#[allow(unused_variables)]
#[tokio::test]
async fn test_add_sale_item_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "A".to_string(), None, Some(1900)).await.unwrap();
    queries::create_customer(&pool, "Customer".to_string(), "c@example.com".to_string()).await.unwrap();
    queries::create_book(&pool, 1, "Book".to_string(), "sci-fi".to_string(), 10.0, None).await.unwrap();
    queries::create_sale(&pool, 1).await.unwrap();
    queries::add_sale_item(&pool, 1, 1, 3, 10.0).await.unwrap();
}

// ─── :execrows queries ────────────────────────────────────────────────────────────

#[allow(unused_variables)]
#[tokio::test]
async fn test_delete_book_by_id_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "A".to_string(), None, Some(1900)).await.unwrap();
    queries::create_book(&pool, 1, "Book1".to_string(), "sci-fi".to_string(), 10.0, None).await.unwrap();
    let affected = queries::delete_book_by_id(&pool, 1).await.unwrap();
    assert_eq!(affected, 1);
}

// ─── :many queries ────────────────────────────────────────────────────────────

#[allow(unused_variables)]
#[tokio::test]
async fn test_list_authors_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "Asimov".to_string(), None, Some(1920)).await.unwrap();
    queries::create_author(&pool, "Herbert".to_string(), None, Some(1920)).await.unwrap();
    let authors = queries::list_authors(&pool).await.unwrap();
    assert_eq!(authors.len(), 2);
    assert_eq!(authors[0].name, "Asimov".to_string());
    assert_eq!(authors[1].name, "Herbert".to_string());
}

#[allow(unused_variables)]
#[tokio::test]
async fn test_list_books_by_genre_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "A".to_string(), None, Some(1900)).await.unwrap();
    queries::create_book(&pool, 1, "SciFi1".to_string(), "sci-fi".to_string(), 9.99, None).await.unwrap();
    queries::create_book(&pool, 1, "Fantasy1".to_string(), "fantasy".to_string(), 11.99, None).await.unwrap();
    let books = queries::list_books_by_genre(&pool, "sci-fi".to_string()).await.unwrap();
    assert_eq!(books.len(), 1);
    assert_eq!(books[0].title, "SciFi1".to_string());
}

#[allow(unused_variables)]
#[tokio::test]
async fn test_list_books_by_genre_or_all_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "A".to_string(), None, Some(1900)).await.unwrap();
    queries::create_book(&pool, 1, "B1".to_string(), "sci-fi".to_string(), 9.99, None).await.unwrap();
    queries::create_book(&pool, 1, "B2".to_string(), "fantasy".to_string(), 9.99, None).await.unwrap();
    let all_books = queries::list_books_by_genre_or_all(&pool, None).await.unwrap();
    assert_eq!(all_books.len(), 2);
    let scifi = queries::list_books_by_genre_or_all(&pool, Some("sci-fi".to_string())).await.unwrap();
    assert_eq!(scifi.len(), 1);
}

#[allow(unused_variables)]
#[tokio::test]
async fn test_list_books_with_author_join_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "Author A".to_string(), Some("Bio A".to_string()), Some(1920)).await.unwrap();
    queries::create_book(&pool, 1, "Book A".to_string(), "sci-fi".to_string(), 9.99, None).await.unwrap();
    let rows = queries::list_books_with_author(&pool).await.unwrap();
    assert_eq!(rows.len(), 1);
    assert_eq!(rows[0].author_name, "Author A".to_string());
}

// ─── :one queries ────────────────────────────────────────────────────────────

#[allow(unused_variables)]
#[tokio::test]
async fn test_get_author_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "Asimov".to_string(), Some("Sci-fi master".to_string()), Some(1920)).await.unwrap();
    let author = queries::get_author(&pool, 1).await.unwrap();
    let author = author.expect("expected non-nil author");
    assert_eq!(author.name, "Asimov".to_string());
    assert_eq!(author.bio, Some("Sci-fi master".to_string()));
    assert_eq!(author.birth_year, Some(1920));
}

#[allow(unused_variables)]
#[tokio::test]
async fn test_get_author_not_found_gen() {
    let pool = gen_setup_db().await;
    let result = queries::get_author(&pool, 999).await.unwrap();
    assert!(result.is_none());
}

#[allow(unused_variables)]
#[tokio::test]
async fn test_get_book_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "Herbert".to_string(), None, Some(1920)).await.unwrap();
    queries::create_book(&pool, 1, "Dune".to_string(), "sci-fi".to_string(), 12.99, Some("1965-06-01".to_string())).await.unwrap();
    let book = queries::get_book(&pool, 1).await.unwrap();
    let book = book.expect("expected non-nil book");
    assert_eq!(book.title, "Dune".to_string());
    assert_eq!(book.genre, "sci-fi".to_string());
}

// ─── aggregates ────────────────────────────────────────────────────────────

#[allow(unused_variables)]
#[tokio::test]
async fn test_count_books_by_genre_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "A".to_string(), None, Some(1900)).await.unwrap();
    queries::create_book(&pool, 1, "B1".to_string(), "sci-fi".to_string(), 10.0, None).await.unwrap();
    queries::create_book(&pool, 1, "B2".to_string(), "sci-fi".to_string(), 10.0, None).await.unwrap();
    queries::create_book(&pool, 1, "B3".to_string(), "fantasy".to_string(), 10.0, None).await.unwrap();
    let counts = queries::count_books_by_genre(&pool).await.unwrap();
    assert_eq!(counts.len(), 2);
}

// ─── filtering ────────────────────────────────────────────────────────────

#[allow(unused_variables)]
#[tokio::test]
async fn test_get_books_by_price_range_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "A".to_string(), None, Some(1900)).await.unwrap();
    queries::create_book(&pool, 1, "Cheap".to_string(), "sci-fi".to_string(), 5.99, None).await.unwrap();
    queries::create_book(&pool, 1, "Mid".to_string(), "sci-fi".to_string(), 10.5, None).await.unwrap();
    queries::create_book(&pool, 1, "Expensive".to_string(), "sci-fi".to_string(), 20.99, None).await.unwrap();
    let books = queries::get_books_by_price_range(&pool, 7.0, 15.0).await.unwrap();
    assert_eq!(books.len(), 1);
    assert_eq!(books[0].title, "Mid".to_string());
}

#[allow(unused_variables)]
#[tokio::test]
async fn test_search_books_by_title_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "A".to_string(), None, Some(1900)).await.unwrap();
    queries::create_book(&pool, 1, "Foundation".to_string(), "sci-fi".to_string(), 9.99, None).await.unwrap();
    queries::create_book(&pool, 1, "Dune".to_string(), "sci-fi".to_string(), 12.99, None).await.unwrap();
    let books = queries::search_books_by_title(&pool, "%ound%".to_string()).await.unwrap();
    assert_eq!(books.len(), 1);
    assert_eq!(books[0].title, "Foundation".to_string());
}

// ─── NULL handling ────────────────────────────────────────────────────────────

#[allow(unused_variables)]
#[tokio::test]
async fn test_authors_with_null_bio_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "WithBio".to_string(), Some("Has bio".to_string()), Some(1900)).await.unwrap();
    queries::create_author(&pool, "NoBio".to_string(), None, Some(1900)).await.unwrap();
    let authors = queries::get_authors_with_null_bio(&pool).await.unwrap();
    assert_eq!(authors.len(), 1);
    assert_eq!(authors[0].name, "NoBio".to_string());
}

#[allow(unused_variables)]
#[tokio::test]
async fn test_authors_with_bio_gen() {
    let pool = gen_setup_db().await;
    queries::create_author(&pool, "WithBio".to_string(), Some("Bio".to_string()), Some(1900)).await.unwrap();
    queries::create_author(&pool, "NoBio".to_string(), None, Some(1900)).await.unwrap();
    let authors = queries::get_authors_with_bio(&pool).await.unwrap();
    assert_eq!(authors.len(), 1);
    assert_eq!(authors[0].name, "WithBio".to_string());
}

