include:
  - ../../../docker-compose.yml

services:
  test:
    build: .
    environment:
      DATABASE_URL: postgres://sqltgen:sqltgen@postgres/sqltgen_e2e
    volumes:
      - ../../../../fixtures/bookstore/postgresql/schema.sql:/fixtures/schema.sql:ro
    depends_on:
      postgres:
        condition: service_healthy
