include:
  - ../../../docker-compose.yml

services:
  test:
    build: .
    environment:
      DATABASE_URL: mysql://sqltgen:sqltgen@mysql/sqltgen_e2e
    volumes:
      - ../../../../fixtures/bookstore/mysql/schema.sql:/fixtures/schema.sql:ro
    depends_on:
      mysql:
        condition: service_healthy
