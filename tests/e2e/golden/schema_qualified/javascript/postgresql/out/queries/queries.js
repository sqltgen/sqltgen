// Generated by sqltgen. Do not edit.
// Runtime: pg (node-postgres) — npm install pg
import { releaseDb } from '../sqltgen.js';
/** @typedef {import('../sqltgen.js').Db} Db */
/** @typedef {import('../sqltgen.js').ConnectFn} ConnectFn */

/** @typedef {import('../models/internal_audit_log.js').Internal_AuditLog} Internal_AuditLog */
/** @typedef {import('../models/users.js').Users} Users */

const SQL_GET_USER = `SELECT * FROM public.users WHERE id = $1`;
const SQL_LIST_AUDIT_LOGS = `SELECT * FROM internal.audit_log ORDER BY created_at DESC`;
const SQL_CREATE_AUDIT_LOG = `INSERT INTO internal.audit_log (user_id, action) VALUES ($1, $2)`;

/**
 * @param {Db} db
 * @param {number} id
 * @returns {Promise<Users | null>}
 */
export async function getUser(db, id) {
  const result = await db.query(SQL_GET_USER, [id]);
  return result.rows[0] ?? null;
}

/**
 * @param {Db} db
 * @returns {Promise<Internal_AuditLog[]>}
 */
export async function listAuditLogs(db) {
  const result = await db.query(SQL_LIST_AUDIT_LOGS, []);
  return result.rows;
}

/**
 * @param {Db} db
 * @param {number} userId
 * @param {string} action
 * @returns {Promise<void>}
 */
export async function createAuditLog(db, userId, action) {
  await db.query(SQL_CREATE_AUDIT_LOG, [userId, action]);
}

export class Querier {
  /** @param {ConnectFn} connect */
  constructor(connect) {
    this.connect = connect;
  }

  async getUser(id) {
    const db = await this.connect();
    try {
      return getUser(db, id);
    } finally {
      await releaseDb(db);
    }
  }

  async listAuditLogs() {
    const db = await this.connect();
    try {
      return listAuditLogs(db);
    } finally {
      await releaseDb(db);
    }
  }

  async createAuditLog(userId, action) {
    const db = await this.connect();
    try {
      return createAuditLog(db, userId, action);
    } finally {
      await releaseDb(db);
    }
  }
}
