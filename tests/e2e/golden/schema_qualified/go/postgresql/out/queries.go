// Code generated by sqltgen. Do not edit.
package db

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5"
)

const SQL_GET_USER = `
SELECT * FROM public.users WHERE id = $1
`
const SQL_LIST_AUDIT_LOGS = `
SELECT * FROM internal.audit_log ORDER BY created_at DESC
`
const SQL_CREATE_AUDIT_LOG = `
INSERT INTO internal.audit_log (user_id, action) VALUES ($1, $2)
`

// GetUser executes the GetUser query.
func GetUser(ctx context.Context, db DBTX, id int64) (*Users, error) {
	row := db.QueryRow(ctx, SQL_GET_USER, id)
	var r Users
	err := row.Scan(&r.Id, &r.Name, &r.Email)
	if err == pgx.ErrNoRows {
		return nil, nil
	}
	if err != nil {
		return nil, err
	}
	return &r, nil
}

// ListAuditLogs executes the ListAuditLogs query.
func ListAuditLogs(ctx context.Context, db DBTX) ([]Internal_AuditLog, error) {
	rows, err := db.Query(ctx, SQL_LIST_AUDIT_LOGS)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var results []Internal_AuditLog
	for rows.Next() {
		var r Internal_AuditLog
		if err := rows.Scan(&r.Id, &r.UserId, &r.Action, &r.CreatedAt); err != nil {
			return nil, err
		}
		results = append(results, r)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return results, nil
}

// CreateAuditLog executes the CreateAuditLog query.
func CreateAuditLog(ctx context.Context, db DBTX, user_id int64, action string) error {
	_, err := db.Exec(ctx, SQL_CREATE_AUDIT_LOG, user_id, action)
	return err
}

// Querier wraps a DBTX and exposes named query methods.
type Querier struct {
	db DBTX
}

// NewQuerier returns a new Querier backed by db.
func NewQuerier(db DBTX) *Querier {
	return &Querier{db: db}
}

// GetUser delegates to the package-level GetUser function.
func (q *Querier) GetUser(ctx context.Context, id int64) (*Users, error) {
	return GetUser(ctx, q.db, id)
}

// ListAuditLogs delegates to the package-level ListAuditLogs function.
func (q *Querier) ListAuditLogs(ctx context.Context) ([]Internal_AuditLog, error) {
	return ListAuditLogs(ctx, q.db)
}

// CreateAuditLog delegates to the package-level CreateAuditLog function.
func (q *Querier) CreateAuditLog(ctx context.Context, user_id int64, action string) error {
	return CreateAuditLog(ctx, q.db, user_id, action)
}
