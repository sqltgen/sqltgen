// Generated by sqltgen. Do not edit.
#include "queries.hpp"

namespace db {

std::optional<Author> create_author(pqxx::connection& db, const std::string& name, const std::optional<std::string>& bio, const std::optional<std::int32_t>& birth_year) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::int64_t, std::string, std::optional<std::string>, std::optional<std::int32_t>>(SQL_CREATE_AUTHOR, pqxx::params{name, bio, birth_year});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_, name_, bio_, birth_year_] = *opt;
    return Author{std::move(id_), std::move(name_), std::move(bio_), std::move(birth_year_)};
}

std::optional<Author> get_author(pqxx::connection& db, std::int64_t id) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::int64_t, std::string, std::optional<std::string>, std::optional<std::int32_t>>(SQL_GET_AUTHOR, pqxx::params{id});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_, name_, bio_, birth_year_] = *opt;
    return Author{std::move(id_), std::move(name_), std::move(bio_), std::move(birth_year_)};
}

std::vector<Author> list_authors(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<Author> rows;
    for (auto [id_, name_, bio_, birth_year_] : txn.query<std::int64_t, std::string, std::optional<std::string>, std::optional<std::int32_t>>(SQL_LIST_AUTHORS)) {
        rows.push_back(Author{std::move(id_), std::move(name_), std::move(bio_), std::move(birth_year_)});
    }
    txn.commit();
    return rows;
}

std::optional<Author> update_author_bio(pqxx::connection& db, const std::optional<std::string>& bio, std::int64_t id) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::int64_t, std::string, std::optional<std::string>, std::optional<std::int32_t>>(SQL_UPDATE_AUTHOR_BIO, pqxx::params{bio, id});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_, name_, bio_, birth_year_] = *opt;
    return Author{std::move(id_), std::move(name_), std::move(bio_), std::move(birth_year_)};
}

std::optional<DeleteAuthorRow> delete_author(pqxx::connection& db, std::int64_t id) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::int64_t, std::string>(SQL_DELETE_AUTHOR, pqxx::params{id});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_, name_] = *opt;
    return DeleteAuthorRow{std::move(id_), std::move(name_)};
}

std::optional<Book> create_book(pqxx::connection& db, std::int64_t author_id, const std::string& title, const std::string& genre, const std::string& price, const std::optional<std::string>& published_at) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::int64_t, std::int64_t, std::string, std::string, std::string, std::optional<std::string>>(SQL_CREATE_BOOK, pqxx::params{author_id, title, genre, price, published_at});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_, author_id_, title_, genre_, price_, published_at_] = *opt;
    return Book{std::move(id_), std::move(author_id_), std::move(title_), std::move(genre_), std::move(price_), std::move(published_at_)};
}

std::optional<Book> get_book(pqxx::connection& db, std::int64_t id) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::int64_t, std::int64_t, std::string, std::string, std::string, std::optional<std::string>>(SQL_GET_BOOK, pqxx::params{id});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_, author_id_, title_, genre_, price_, published_at_] = *opt;
    return Book{std::move(id_), std::move(author_id_), std::move(title_), std::move(genre_), std::move(price_), std::move(published_at_)};
}

std::vector<Book> get_books_by_ids(pqxx::connection& db, const std::vector<std::int64_t>& ids) {
    pqxx::work txn(db);
    std::vector<Book> rows;
    for (auto [id_, author_id_, title_, genre_, price_, published_at_] : txn.query<std::int64_t, std::int64_t, std::string, std::string, std::string, std::optional<std::string>>(SQL_GET_BOOKS_BY_IDS, pqxx::params{ids})) {
        rows.push_back(Book{std::move(id_), std::move(author_id_), std::move(title_), std::move(genre_), std::move(price_), std::move(published_at_)});
    }
    txn.commit();
    return rows;
}

std::vector<Book> list_books_by_genre(pqxx::connection& db, const std::string& genre) {
    pqxx::work txn(db);
    std::vector<Book> rows;
    for (auto [id_, author_id_, title_, genre_, price_, published_at_] : txn.query<std::int64_t, std::int64_t, std::string, std::string, std::string, std::optional<std::string>>(SQL_LIST_BOOKS_BY_GENRE, pqxx::params{genre})) {
        rows.push_back(Book{std::move(id_), std::move(author_id_), std::move(title_), std::move(genre_), std::move(price_), std::move(published_at_)});
    }
    txn.commit();
    return rows;
}

std::vector<Book> list_books_by_genre_or_all(pqxx::connection& db, const std::string& genre) {
    pqxx::work txn(db);
    std::vector<Book> rows;
    for (auto [id_, author_id_, title_, genre_, price_, published_at_] : txn.query<std::int64_t, std::int64_t, std::string, std::string, std::string, std::optional<std::string>>(SQL_LIST_BOOKS_BY_GENRE_OR_ALL, pqxx::params{genre})) {
        rows.push_back(Book{std::move(id_), std::move(author_id_), std::move(title_), std::move(genre_), std::move(price_), std::move(published_at_)});
    }
    txn.commit();
    return rows;
}

std::optional<CreateCustomerRow> create_customer(pqxx::connection& db, const std::string& name, const std::string& email) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::int64_t>(SQL_CREATE_CUSTOMER, pqxx::params{name, email});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_] = *opt;
    return CreateCustomerRow{std::move(id_)};
}

std::optional<CreateSaleRow> create_sale(pqxx::connection& db, std::int64_t customer_id) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::int64_t>(SQL_CREATE_SALE, pqxx::params{customer_id});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_] = *opt;
    return CreateSaleRow{std::move(id_)};
}

void add_sale_item(pqxx::connection& db, std::int64_t sale_id, std::int64_t book_id, std::int32_t quantity, const std::string& unit_price) {
    pqxx::work txn(db);
    txn.exec(SQL_ADD_SALE_ITEM, pqxx::params{sale_id, book_id, quantity, unit_price}).no_rows();
    txn.commit();
}

std::vector<ListBooksWithAuthorRow> list_books_with_author(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<ListBooksWithAuthorRow> rows;
    for (auto [id_, title_, genre_, price_, published_at_, author_name_, author_bio_] : txn.query<std::int64_t, std::string, std::string, std::string, std::optional<std::string>, std::string, std::optional<std::string>>(SQL_LIST_BOOKS_WITH_AUTHOR)) {
        rows.push_back(ListBooksWithAuthorRow{std::move(id_), std::move(title_), std::move(genre_), std::move(price_), std::move(published_at_), std::move(author_name_), std::move(author_bio_)});
    }
    txn.commit();
    return rows;
}

std::vector<BookSummaries> list_book_summaries_view(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<BookSummaries> rows;
    for (auto [id_, title_, genre_, author_name_] : txn.query<std::int64_t, std::string, std::string, std::string>(SQL_LIST_BOOK_SUMMARIES_VIEW)) {
        rows.push_back(BookSummaries{std::move(id_), std::move(title_), std::move(genre_), std::move(author_name_)});
    }
    txn.commit();
    return rows;
}

std::vector<Book> get_books_never_ordered(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<Book> rows;
    for (auto [id_, author_id_, title_, genre_, price_, published_at_] : txn.query<std::int64_t, std::int64_t, std::string, std::string, std::string, std::optional<std::string>>(SQL_GET_BOOKS_NEVER_ORDERED)) {
        rows.push_back(Book{std::move(id_), std::move(author_id_), std::move(title_), std::move(genre_), std::move(price_), std::move(published_at_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetTopSellingBooksRow> get_top_selling_books(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<GetTopSellingBooksRow> rows;
    for (auto [id_, title_, genre_, price_, units_sold_] : txn.query<std::int64_t, std::string, std::string, std::string, std::optional<std::int64_t>>(SQL_GET_TOP_SELLING_BOOKS)) {
        rows.push_back(GetTopSellingBooksRow{std::move(id_), std::move(title_), std::move(genre_), std::move(price_), std::move(units_sold_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBestCustomersRow> get_best_customers(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<GetBestCustomersRow> rows;
    for (auto [id_, name_, email_, total_spent_] : txn.query<std::int64_t, std::string, std::string, std::optional<std::string>>(SQL_GET_BEST_CUSTOMERS)) {
        rows.push_back(GetBestCustomersRow{std::move(id_), std::move(name_), std::move(email_), std::move(total_spent_)});
    }
    txn.commit();
    return rows;
}

std::vector<CountBooksByGenreRow> count_books_by_genre(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<CountBooksByGenreRow> rows;
    for (auto [genre_, book_count_] : txn.query<std::string, std::int64_t>(SQL_COUNT_BOOKS_BY_GENRE)) {
        rows.push_back(CountBooksByGenreRow{std::move(genre_), std::move(book_count_)});
    }
    txn.commit();
    return rows;
}

std::vector<ListBooksWithLimitRow> list_books_with_limit(pqxx::connection& db, std::int64_t limit, std::int64_t offset) {
    pqxx::work txn(db);
    std::vector<ListBooksWithLimitRow> rows;
    for (auto [id_, title_, genre_, price_] : txn.query<std::int64_t, std::string, std::string, std::string>(SQL_LIST_BOOKS_WITH_LIMIT, pqxx::params{limit, offset})) {
        rows.push_back(ListBooksWithLimitRow{std::move(id_), std::move(title_), std::move(genre_), std::move(price_)});
    }
    txn.commit();
    return rows;
}

std::vector<SearchBooksByTitleRow> search_books_by_title(pqxx::connection& db, const std::string& title) {
    pqxx::work txn(db);
    std::vector<SearchBooksByTitleRow> rows;
    for (auto [id_, title_, genre_, price_] : txn.query<std::int64_t, std::string, std::string, std::string>(SQL_SEARCH_BOOKS_BY_TITLE, pqxx::params{title})) {
        rows.push_back(SearchBooksByTitleRow{std::move(id_), std::move(title_), std::move(genre_), std::move(price_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBooksByPriceRangeRow> get_books_by_price_range(pqxx::connection& db, const std::string& price, const std::string& price_2) {
    pqxx::work txn(db);
    std::vector<GetBooksByPriceRangeRow> rows;
    for (auto [id_, title_, genre_, price_] : txn.query<std::int64_t, std::string, std::string, std::string>(SQL_GET_BOOKS_BY_PRICE_RANGE, pqxx::params{price, price_2})) {
        rows.push_back(GetBooksByPriceRangeRow{std::move(id_), std::move(title_), std::move(genre_), std::move(price_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBooksInGenresRow> get_books_in_genres(pqxx::connection& db, const std::string& genre, const std::string& genre_2, const std::string& genre_3) {
    pqxx::work txn(db);
    std::vector<GetBooksInGenresRow> rows;
    for (auto [id_, title_, genre_, price_] : txn.query<std::int64_t, std::string, std::string, std::string>(SQL_GET_BOOKS_IN_GENRES, pqxx::params{genre, genre_2, genre_3})) {
        rows.push_back(GetBooksInGenresRow{std::move(id_), std::move(title_), std::move(genre_), std::move(price_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBookPriceLabelRow> get_book_price_label(pqxx::connection& db, const std::string& price) {
    pqxx::work txn(db);
    std::vector<GetBookPriceLabelRow> rows;
    for (auto [id_, title_, price_, price_label_] : txn.query<std::int64_t, std::string, std::string, std::string>(SQL_GET_BOOK_PRICE_LABEL, pqxx::params{price})) {
        rows.push_back(GetBookPriceLabelRow{std::move(id_), std::move(title_), std::move(price_), std::move(price_label_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBookPriceOrDefaultRow> get_book_price_or_default(pqxx::connection& db, const std::optional<std::string>& price) {
    pqxx::work txn(db);
    std::vector<GetBookPriceOrDefaultRow> rows;
    for (auto [id_, title_, effective_price_] : txn.query<std::int64_t, std::string, std::string>(SQL_GET_BOOK_PRICE_OR_DEFAULT, pqxx::params{price})) {
        rows.push_back(GetBookPriceOrDefaultRow{std::move(id_), std::move(title_), std::move(effective_price_)});
    }
    txn.commit();
    return rows;
}

std::int64_t delete_book_by_id(pqxx::connection& db, std::int64_t id) {
    pqxx::work txn(db);
    auto affected = txn.exec(SQL_DELETE_BOOK_BY_ID, pqxx::params{id}).affected_rows();
    txn.commit();
    return static_cast<std::int64_t>(affected);
}

std::vector<GetGenresWithManyBooksRow> get_genres_with_many_books(pqxx::connection& db, std::int64_t count) {
    pqxx::work txn(db);
    std::vector<GetGenresWithManyBooksRow> rows;
    for (auto [genre_, book_count_] : txn.query<std::string, std::int64_t>(SQL_GET_GENRES_WITH_MANY_BOOKS, pqxx::params{count})) {
        rows.push_back(GetGenresWithManyBooksRow{std::move(genre_), std::move(book_count_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBooksByAuthorParamRow> get_books_by_author_param(pqxx::connection& db, const std::optional<std::int32_t>& birth_year) {
    pqxx::work txn(db);
    std::vector<GetBooksByAuthorParamRow> rows;
    for (auto [id_, title_, price_] : txn.query<std::int64_t, std::string, std::string>(SQL_GET_BOOKS_BY_AUTHOR_PARAM, pqxx::params{birth_year})) {
        rows.push_back(GetBooksByAuthorParamRow{std::move(id_), std::move(title_), std::move(price_)});
    }
    txn.commit();
    return rows;
}

std::vector<Book> get_all_book_fields(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<Book> rows;
    for (auto [id_, author_id_, title_, genre_, price_, published_at_] : txn.query<std::int64_t, std::int64_t, std::string, std::string, std::string, std::optional<std::string>>(SQL_GET_ALL_BOOK_FIELDS)) {
        rows.push_back(Book{std::move(id_), std::move(author_id_), std::move(title_), std::move(genre_), std::move(price_), std::move(published_at_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBooksNotByAuthorRow> get_books_not_by_author(pqxx::connection& db, const std::string& name) {
    pqxx::work txn(db);
    std::vector<GetBooksNotByAuthorRow> rows;
    for (auto [id_, title_, genre_] : txn.query<std::int64_t, std::string, std::string>(SQL_GET_BOOKS_NOT_BY_AUTHOR, pqxx::params{name})) {
        rows.push_back(GetBooksNotByAuthorRow{std::move(id_), std::move(title_), std::move(genre_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBooksWithRecentSalesRow> get_books_with_recent_sales(pqxx::connection& db, const std::string& ordered_at) {
    pqxx::work txn(db);
    std::vector<GetBooksWithRecentSalesRow> rows;
    for (auto [id_, title_, genre_] : txn.query<std::int64_t, std::string, std::string>(SQL_GET_BOOKS_WITH_RECENT_SALES, pqxx::params{ordered_at})) {
        rows.push_back(GetBooksWithRecentSalesRow{std::move(id_), std::move(title_), std::move(genre_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBookWithAuthorNameRow> get_book_with_author_name(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<GetBookWithAuthorNameRow> rows;
    for (auto [id_, title_, author_name_] : txn.query<std::int64_t, std::string, std::optional<std::string>>(SQL_GET_BOOK_WITH_AUTHOR_NAME)) {
        rows.push_back(GetBookWithAuthorNameRow{std::move(id_), std::move(title_), std::move(author_name_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetAuthorStatsRow> get_author_stats(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<GetAuthorStatsRow> rows;
    for (auto [id_, name_, num_books_, total_sold_] : txn.query<std::int64_t, std::string, std::int64_t, std::int64_t>(SQL_GET_AUTHOR_STATS)) {
        rows.push_back(GetAuthorStatsRow{std::move(id_), std::move(name_), std::move(num_books_), std::move(total_sold_)});
    }
    txn.commit();
    return rows;
}

std::vector<ArchiveAndReturnBooksRow> archive_and_return_books(pqxx::connection& db, const std::optional<std::string>& published_at) {
    pqxx::work txn(db);
    std::vector<ArchiveAndReturnBooksRow> rows;
    for (auto [id_, title_, genre_, price_] : txn.query<std::int64_t, std::string, std::string, std::string>(SQL_ARCHIVE_AND_RETURN_BOOKS, pqxx::params{published_at})) {
        rows.push_back(ArchiveAndReturnBooksRow{std::move(id_), std::move(title_), std::move(genre_), std::move(price_)});
    }
    txn.commit();
    return rows;
}

std::optional<Product> get_product(pqxx::connection& db, const std::string& id) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::string, std::string, std::string, bool, std::optional<float>, std::optional<double>, std::string, std::optional<std::string>, std::optional<pqxx::bytes>, std::string, std::int16_t>(SQL_GET_PRODUCT, pqxx::params{id});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_, sku_, name_, active_, weight_kg_, rating_, tags_, metadata_, thumbnail_, created_at_, stock_count_] = *opt;
    return Product{std::move(id_), std::move(sku_), std::move(name_), std::move(active_), std::move(weight_kg_), std::move(rating_), std::move(tags_), std::move(metadata_), std::move(thumbnail_), std::move(created_at_), std::move(stock_count_)};
}

std::vector<ListActiveProductsRow> list_active_products(pqxx::connection& db, bool active) {
    pqxx::work txn(db);
    std::vector<ListActiveProductsRow> rows;
    for (auto [id_, sku_, name_, active_, weight_kg_, rating_, tags_, metadata_, created_at_, stock_count_] : txn.query<std::string, std::string, std::string, bool, std::optional<float>, std::optional<double>, std::string, std::optional<std::string>, std::string, std::int16_t>(SQL_LIST_ACTIVE_PRODUCTS, pqxx::params{active})) {
        rows.push_back(ListActiveProductsRow{std::move(id_), std::move(sku_), std::move(name_), std::move(active_), std::move(weight_kg_), std::move(rating_), std::move(tags_), std::move(metadata_), std::move(created_at_), std::move(stock_count_)});
    }
    txn.commit();
    return rows;
}

std::optional<Product> insert_product(pqxx::connection& db, const std::string& id, const std::string& sku, const std::string& name, bool active, const std::optional<float>& weight_kg, const std::optional<double>& rating, const std::string& tags, const std::optional<std::string>& metadata, const std::optional<pqxx::bytes>& thumbnail, std::int16_t stock_count) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::string, std::string, std::string, bool, std::optional<float>, std::optional<double>, std::string, std::optional<std::string>, std::optional<pqxx::bytes>, std::string, std::int16_t>(SQL_INSERT_PRODUCT, pqxx::params{id, sku, name, active, weight_kg, rating, tags, metadata, thumbnail, stock_count});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_, sku_, name_, active_, weight_kg_, rating_, tags_, metadata_, thumbnail_, created_at_, stock_count_] = *opt;
    return Product{std::move(id_), std::move(sku_), std::move(name_), std::move(active_), std::move(weight_kg_), std::move(rating_), std::move(tags_), std::move(metadata_), std::move(thumbnail_), std::move(created_at_), std::move(stock_count_)};
}

std::vector<GetAuthorsWithNullBioRow> get_authors_with_null_bio(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<GetAuthorsWithNullBioRow> rows;
    for (auto [id_, name_, birth_year_] : txn.query<std::int64_t, std::string, std::optional<std::int32_t>>(SQL_GET_AUTHORS_WITH_NULL_BIO)) {
        rows.push_back(GetAuthorsWithNullBioRow{std::move(id_), std::move(name_), std::move(birth_year_)});
    }
    txn.commit();
    return rows;
}

std::vector<Author> get_authors_with_bio(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<Author> rows;
    for (auto [id_, name_, bio_, birth_year_] : txn.query<std::int64_t, std::string, std::optional<std::string>, std::optional<std::int32_t>>(SQL_GET_AUTHORS_WITH_BIO)) {
        rows.push_back(Author{std::move(id_), std::move(name_), std::move(bio_), std::move(birth_year_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBooksPublishedBetweenRow> get_books_published_between(pqxx::connection& db, const std::optional<std::string>& published_at, const std::optional<std::string>& published_at_2) {
    pqxx::work txn(db);
    std::vector<GetBooksPublishedBetweenRow> rows;
    for (auto [id_, title_, genre_, price_, published_at_] : txn.query<std::int64_t, std::string, std::string, std::string, std::optional<std::string>>(SQL_GET_BOOKS_PUBLISHED_BETWEEN, pqxx::params{published_at, published_at_2})) {
        rows.push_back(GetBooksPublishedBetweenRow{std::move(id_), std::move(title_), std::move(genre_), std::move(price_), std::move(published_at_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetDistinctGenresRow> get_distinct_genres(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<GetDistinctGenresRow> rows;
    for (auto [genre_] : txn.query<std::string>(SQL_GET_DISTINCT_GENRES)) {
        rows.push_back(GetDistinctGenresRow{std::move(genre_)});
    }
    txn.commit();
    return rows;
}

std::vector<GetBooksWithSalesCountRow> get_books_with_sales_count(pqxx::connection& db) {
    pqxx::work txn(db);
    std::vector<GetBooksWithSalesCountRow> rows;
    for (auto [id_, title_, genre_, total_quantity_] : txn.query<std::int64_t, std::string, std::string, std::int64_t>(SQL_GET_BOOKS_WITH_SALES_COUNT)) {
        rows.push_back(GetBooksWithSalesCountRow{std::move(id_), std::move(title_), std::move(genre_), std::move(total_quantity_)});
    }
    txn.commit();
    return rows;
}

std::optional<CountSaleItemsRow> count_sale_items(pqxx::connection& db, std::int64_t sale_id) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::int64_t>(SQL_COUNT_SALE_ITEMS, pqxx::params{sale_id});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [item_count_] = *opt;
    return CountSaleItemsRow{std::move(item_count_)};
}

std::optional<UpsertProductRow> upsert_product(pqxx::connection& db, const std::string& id, const std::string& sku, const std::string& name, bool active, const std::string& tags, std::int16_t stock_count) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::string, std::string, std::string, bool, std::string, std::int16_t>(SQL_UPSERT_PRODUCT, pqxx::params{id, sku, name, active, tags, stock_count});
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [id_, sku_, name_, active_, tags_, stock_count_] = *opt;
    return UpsertProductRow{std::move(id_), std::move(sku_), std::move(name_), std::move(active_), std::move(tags_), std::move(stock_count_)};
}

std::optional<GetSaleItemQuantityAggregatesRow> get_sale_item_quantity_aggregates(pqxx::connection& db) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::optional<std::int32_t>, std::optional<std::int32_t>, std::optional<std::int64_t>, std::optional<std::string>>(SQL_GET_SALE_ITEM_QUANTITY_AGGREGATES);
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [min_qty_, max_qty_, sum_qty_, avg_qty_] = *opt;
    return GetSaleItemQuantityAggregatesRow{std::move(min_qty_), std::move(max_qty_), std::move(sum_qty_), std::move(avg_qty_)};
}

std::optional<GetBookPriceAggregatesRow> get_book_price_aggregates(pqxx::connection& db) {
    pqxx::work txn(db);
    auto opt = txn.query01<std::optional<std::string>, std::optional<std::string>, std::optional<std::string>, std::optional<std::string>>(SQL_GET_BOOK_PRICE_AGGREGATES);
    txn.commit();
    if (!opt) return std::nullopt;
    auto& [min_price_, max_price_, sum_price_, avg_price_] = *opt;
    return GetBookPriceAggregatesRow{std::move(min_price_), std::move(max_price_), std::move(sum_price_), std::move(avg_price_)};
}


std::optional<Author> Querier::create_author(const std::string& name, const std::optional<std::string>& bio, const std::optional<std::int32_t>& birth_year) {
    return ::db::create_author(db_, name, bio, birth_year);
}

std::optional<Author> Querier::get_author(std::int64_t id) {
    return ::db::get_author(db_, id);
}

std::vector<Author> Querier::list_authors() {
    return ::db::list_authors(db_);
}

std::optional<Author> Querier::update_author_bio(const std::optional<std::string>& bio, std::int64_t id) {
    return ::db::update_author_bio(db_, bio, id);
}

std::optional<DeleteAuthorRow> Querier::delete_author(std::int64_t id) {
    return ::db::delete_author(db_, id);
}

std::optional<Book> Querier::create_book(std::int64_t author_id, const std::string& title, const std::string& genre, const std::string& price, const std::optional<std::string>& published_at) {
    return ::db::create_book(db_, author_id, title, genre, price, published_at);
}

std::optional<Book> Querier::get_book(std::int64_t id) {
    return ::db::get_book(db_, id);
}

std::vector<Book> Querier::get_books_by_ids(const std::vector<std::int64_t>& ids) {
    return ::db::get_books_by_ids(db_, ids);
}

std::vector<Book> Querier::list_books_by_genre(const std::string& genre) {
    return ::db::list_books_by_genre(db_, genre);
}

std::vector<Book> Querier::list_books_by_genre_or_all(const std::string& genre) {
    return ::db::list_books_by_genre_or_all(db_, genre);
}

std::optional<CreateCustomerRow> Querier::create_customer(const std::string& name, const std::string& email) {
    return ::db::create_customer(db_, name, email);
}

std::optional<CreateSaleRow> Querier::create_sale(std::int64_t customer_id) {
    return ::db::create_sale(db_, customer_id);
}

void Querier::add_sale_item(std::int64_t sale_id, std::int64_t book_id, std::int32_t quantity, const std::string& unit_price) {
    return ::db::add_sale_item(db_, sale_id, book_id, quantity, unit_price);
}

std::vector<ListBooksWithAuthorRow> Querier::list_books_with_author() {
    return ::db::list_books_with_author(db_);
}

std::vector<BookSummaries> Querier::list_book_summaries_view() {
    return ::db::list_book_summaries_view(db_);
}

std::vector<Book> Querier::get_books_never_ordered() {
    return ::db::get_books_never_ordered(db_);
}

std::vector<GetTopSellingBooksRow> Querier::get_top_selling_books() {
    return ::db::get_top_selling_books(db_);
}

std::vector<GetBestCustomersRow> Querier::get_best_customers() {
    return ::db::get_best_customers(db_);
}

std::vector<CountBooksByGenreRow> Querier::count_books_by_genre() {
    return ::db::count_books_by_genre(db_);
}

std::vector<ListBooksWithLimitRow> Querier::list_books_with_limit(std::int64_t limit, std::int64_t offset) {
    return ::db::list_books_with_limit(db_, limit, offset);
}

std::vector<SearchBooksByTitleRow> Querier::search_books_by_title(const std::string& title) {
    return ::db::search_books_by_title(db_, title);
}

std::vector<GetBooksByPriceRangeRow> Querier::get_books_by_price_range(const std::string& price, const std::string& price_2) {
    return ::db::get_books_by_price_range(db_, price, price_2);
}

std::vector<GetBooksInGenresRow> Querier::get_books_in_genres(const std::string& genre, const std::string& genre_2, const std::string& genre_3) {
    return ::db::get_books_in_genres(db_, genre, genre_2, genre_3);
}

std::vector<GetBookPriceLabelRow> Querier::get_book_price_label(const std::string& price) {
    return ::db::get_book_price_label(db_, price);
}

std::vector<GetBookPriceOrDefaultRow> Querier::get_book_price_or_default(const std::optional<std::string>& price) {
    return ::db::get_book_price_or_default(db_, price);
}

std::int64_t Querier::delete_book_by_id(std::int64_t id) {
    return ::db::delete_book_by_id(db_, id);
}

std::vector<GetGenresWithManyBooksRow> Querier::get_genres_with_many_books(std::int64_t count) {
    return ::db::get_genres_with_many_books(db_, count);
}

std::vector<GetBooksByAuthorParamRow> Querier::get_books_by_author_param(const std::optional<std::int32_t>& birth_year) {
    return ::db::get_books_by_author_param(db_, birth_year);
}

std::vector<Book> Querier::get_all_book_fields() {
    return ::db::get_all_book_fields(db_);
}

std::vector<GetBooksNotByAuthorRow> Querier::get_books_not_by_author(const std::string& name) {
    return ::db::get_books_not_by_author(db_, name);
}

std::vector<GetBooksWithRecentSalesRow> Querier::get_books_with_recent_sales(const std::string& ordered_at) {
    return ::db::get_books_with_recent_sales(db_, ordered_at);
}

std::vector<GetBookWithAuthorNameRow> Querier::get_book_with_author_name() {
    return ::db::get_book_with_author_name(db_);
}

std::vector<GetAuthorStatsRow> Querier::get_author_stats() {
    return ::db::get_author_stats(db_);
}

std::vector<ArchiveAndReturnBooksRow> Querier::archive_and_return_books(const std::optional<std::string>& published_at) {
    return ::db::archive_and_return_books(db_, published_at);
}

std::optional<Product> Querier::get_product(const std::string& id) {
    return ::db::get_product(db_, id);
}

std::vector<ListActiveProductsRow> Querier::list_active_products(bool active) {
    return ::db::list_active_products(db_, active);
}

std::optional<Product> Querier::insert_product(const std::string& id, const std::string& sku, const std::string& name, bool active, const std::optional<float>& weight_kg, const std::optional<double>& rating, const std::string& tags, const std::optional<std::string>& metadata, const std::optional<pqxx::bytes>& thumbnail, std::int16_t stock_count) {
    return ::db::insert_product(db_, id, sku, name, active, weight_kg, rating, tags, metadata, thumbnail, stock_count);
}

std::vector<GetAuthorsWithNullBioRow> Querier::get_authors_with_null_bio() {
    return ::db::get_authors_with_null_bio(db_);
}

std::vector<Author> Querier::get_authors_with_bio() {
    return ::db::get_authors_with_bio(db_);
}

std::vector<GetBooksPublishedBetweenRow> Querier::get_books_published_between(const std::optional<std::string>& published_at, const std::optional<std::string>& published_at_2) {
    return ::db::get_books_published_between(db_, published_at, published_at_2);
}

std::vector<GetDistinctGenresRow> Querier::get_distinct_genres() {
    return ::db::get_distinct_genres(db_);
}

std::vector<GetBooksWithSalesCountRow> Querier::get_books_with_sales_count() {
    return ::db::get_books_with_sales_count(db_);
}

std::optional<CountSaleItemsRow> Querier::count_sale_items(std::int64_t sale_id) {
    return ::db::count_sale_items(db_, sale_id);
}

std::optional<UpsertProductRow> Querier::upsert_product(const std::string& id, const std::string& sku, const std::string& name, bool active, const std::string& tags, std::int16_t stock_count) {
    return ::db::upsert_product(db_, id, sku, name, active, tags, stock_count);
}

std::optional<GetSaleItemQuantityAggregatesRow> Querier::get_sale_item_quantity_aggregates() {
    return ::db::get_sale_item_quantity_aggregates(db_);
}

std::optional<GetBookPriceAggregatesRow> Querier::get_book_price_aggregates() {
    return ::db::get_book_price_aggregates(db_);
}

} // namespace db
