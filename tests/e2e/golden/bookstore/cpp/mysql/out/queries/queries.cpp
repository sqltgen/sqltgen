// Generated by sqltgen. Do not edit.
#include "queries.hpp"
#include <cstring>

namespace db {

// ---------- helpers ----------

class MysqlStmt {
    MYSQL_STMT* stmt_;
public:
    MysqlStmt(MYSQL* db, const std::string& sql) : stmt_(mysql_stmt_init(db)) {
        if (!stmt_) throw std::runtime_error(mysql_error(db));
        if (mysql_stmt_prepare(stmt_, sql.c_str(), sql.size()) != 0) {
            std::string err = mysql_stmt_error(stmt_);
            mysql_stmt_close(stmt_);
            throw std::runtime_error(err);
        }
    }
    ~MysqlStmt() { if (stmt_) mysql_stmt_close(stmt_); }
    MysqlStmt(const MysqlStmt&) = delete;
    MysqlStmt& operator=(const MysqlStmt&) = delete;
    void bind_param(MYSQL_BIND* bind) {
        if (mysql_stmt_bind_param(stmt_, bind) != 0)
            throw std::runtime_error(mysql_stmt_error(stmt_));
    }
    void execute() {
        if (mysql_stmt_execute(stmt_) != 0)
            throw std::runtime_error(mysql_stmt_error(stmt_));
    }
    void bind_result(MYSQL_BIND* bind) {
        if (mysql_stmt_bind_result(stmt_, bind) != 0)
            throw std::runtime_error(mysql_stmt_error(stmt_));
    }
    bool fetch_row() {
        int rc = mysql_stmt_fetch(stmt_);
        if (rc == MYSQL_NO_DATA) return false;
        if (rc != 0 && rc != MYSQL_DATA_TRUNCATED)
            throw std::runtime_error(mysql_stmt_error(stmt_));
        return true;
    }
    void fetch_column(MYSQL_BIND* bind, unsigned col) {
        mysql_stmt_fetch_column(stmt_, bind, col, 0);
    }
    my_ulonglong affected_rows() { return mysql_stmt_affected_rows(stmt_); }
};

// ---------- query functions ----------

void create_author(MYSQL* db, const std::string& name, const std::optional<std::string>& bio, const std::optional<std::int32_t>& birth_year) {
    MysqlStmt stmt(db, SQL_CREATE_AUTHOR);

    MYSQL_BIND bind[3];
    memset(bind, 0, sizeof(bind));

    // name — string
    bind[0].buffer = const_cast<char*>(name.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_name_len = name.size();
    bind[0].length = &p_name_len;
    bind[0].buffer_length = p_name_len;

    // bio — string?
    bind[1].buffer = const_cast<char*>(bio.has_value() ? bio.value().c_str() : "");
    bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_bio_len = bio.has_value() ? bio.value().size() : 0;
    bind[1].length = &p_bio_len;
    bind[1].buffer_length = p_bio_len;
    bool p_bio_is_null = !bio.has_value();
    bind[1].is_null = &p_bio_is_null;

    // birth_year — int?
    std::int32_t birth_year_val = birth_year.value_or(std::int32_t{});
    bind[2].buffer = &birth_year_val;
    bind[2].buffer_type = MYSQL_TYPE_LONG;
    bool p_birth_year_is_null = !birth_year.has_value();
    bind[2].is_null = &p_birth_year_is_null;
    stmt.bind_param(bind);

    stmt.execute();
}

std::optional<Author> get_author(MYSQL* db, std::int64_t id) {
    MysqlStmt stmt(db, SQL_GET_AUTHOR);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // id — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&id);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // name — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long name_len = 0;
    result_bind[1].length = &name_len;
    result_bind[1].buffer_length = 0;
    bool name_is_null = false;
    result_bind[1].is_null = &name_is_null;

    // bio — string? (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long bio_len = 0;
    result_bind[2].length = &bio_len;
    result_bind[2].buffer_length = 0;
    bool bio_is_null = false;
    result_bind[2].is_null = &bio_is_null;

    // birth_year — int?
    std::int32_t birth_year_val{};
    result_bind[3].buffer = &birth_year_val;
    result_bind[3].buffer_type = MYSQL_TYPE_LONG;
    bool birth_year_is_null = false;
    result_bind[3].is_null = &birth_year_is_null;
    stmt.bind_result(result_bind);

    if (!stmt.fetch_row()) return std::nullopt;

    std::string name_val(name_len, '\0');
    result_bind[1].buffer = name_val.data();
    result_bind[1].buffer_length = name_len;
    stmt.fetch_column(&result_bind[1], 1);

    std::string bio_val(bio_len, '\0');
    result_bind[2].buffer = bio_val.data();
    result_bind[2].buffer_length = bio_len;
    stmt.fetch_column(&result_bind[2], 2);

    return Author{
        id_val,
        std::move(name_val),
        bio_is_null ? std::nullopt : std::optional<std::string>(std::move(bio_val)),
        birth_year_is_null ? std::nullopt : std::optional<std::int32_t>(birth_year_val)
    };
}

std::vector<Author> list_authors(MYSQL* db) {
    MysqlStmt stmt(db, SQL_LIST_AUTHORS);
    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // name — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long name_len = 0;
    result_bind[1].length = &name_len;
    result_bind[1].buffer_length = 0;
    bool name_is_null = false;
    result_bind[1].is_null = &name_is_null;

    // bio — string? (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long bio_len = 0;
    result_bind[2].length = &bio_len;
    result_bind[2].buffer_length = 0;
    bool bio_is_null = false;
    result_bind[2].is_null = &bio_is_null;

    // birth_year — int?
    std::int32_t birth_year_val{};
    result_bind[3].buffer = &birth_year_val;
    result_bind[3].buffer_type = MYSQL_TYPE_LONG;
    bool birth_year_is_null = false;
    result_bind[3].is_null = &birth_year_is_null;
    stmt.bind_result(result_bind);

    std::vector<Author> rows;
    while (stmt.fetch_row()) {
        std::string name_val(name_len, '\0');
        result_bind[1].buffer = name_val.data();
        result_bind[1].buffer_length = name_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string bio_val(bio_len, '\0');
        result_bind[2].buffer = bio_val.data();
        result_bind[2].buffer_length = bio_len;
        stmt.fetch_column(&result_bind[2], 2);

        rows.push_back(Author{
            id_val,
            std::move(name_val),
            bio_is_null ? std::nullopt : std::optional<std::string>(std::move(bio_val)),
            birth_year_is_null ? std::nullopt : std::optional<std::int32_t>(birth_year_val)
        });
    }
    return rows;
}

void update_author_bio(MYSQL* db, const std::optional<std::string>& bio, std::int64_t id) {
    MysqlStmt stmt(db, SQL_UPDATE_AUTHOR_BIO);

    MYSQL_BIND bind[2];
    memset(bind, 0, sizeof(bind));

    // bio — string?
    bind[0].buffer = const_cast<char*>(bio.has_value() ? bio.value().c_str() : "");
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_bio_len = bio.has_value() ? bio.value().size() : 0;
    bind[0].length = &p_bio_len;
    bind[0].buffer_length = p_bio_len;
    bool p_bio_is_null = !bio.has_value();
    bind[0].is_null = &p_bio_is_null;

    // id — bigint
    bind[1].buffer = const_cast<std::int64_t*>(&id);
    bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    stmt.bind_param(bind);

    stmt.execute();
}

void delete_author(MYSQL* db, std::int64_t id) {
    MysqlStmt stmt(db, SQL_DELETE_AUTHOR);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // id — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&id);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    stmt.bind_param(bind);

    stmt.execute();
}

void create_book(MYSQL* db, std::int64_t author_id, const std::string& title, const std::string& genre, const std::string& price, const std::optional<std::string>& published_at) {
    MysqlStmt stmt(db, SQL_CREATE_BOOK);

    MYSQL_BIND bind[5];
    memset(bind, 0, sizeof(bind));

    // author_id — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&author_id);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;

    // title — string
    bind[1].buffer = const_cast<char*>(title.c_str());
    bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_title_len = title.size();
    bind[1].length = &p_title_len;
    bind[1].buffer_length = p_title_len;

    // genre — string
    bind[2].buffer = const_cast<char*>(genre.c_str());
    bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_genre_len = genre.size();
    bind[2].length = &p_genre_len;
    bind[2].buffer_length = p_genre_len;

    // price — decimal
    bind[3].buffer = const_cast<char*>(price.c_str());
    bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_price_len = price.size();
    bind[3].length = &p_price_len;
    bind[3].buffer_length = p_price_len;

    // published_at — date?
    bind[4].buffer = const_cast<char*>(published_at.has_value() ? published_at.value().c_str() : "");
    bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_published_at_len = published_at.has_value() ? published_at.value().size() : 0;
    bind[4].length = &p_published_at_len;
    bind[4].buffer_length = p_published_at_len;
    bool p_published_at_is_null = !published_at.has_value();
    bind[4].is_null = &p_published_at_is_null;
    stmt.bind_param(bind);

    stmt.execute();
}

std::optional<Book> get_book(MYSQL* db, std::int64_t id) {
    MysqlStmt stmt(db, SQL_GET_BOOK);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // id — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&id);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[6];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // author_id — bigint
    std::int64_t author_id_val{};
    result_bind[1].buffer = &author_id_val;
    result_bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    bool author_id_is_null = false;
    result_bind[1].is_null = &author_id_is_null;

    // title — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[2].length = &title_len;
    result_bind[2].buffer_length = 0;
    bool title_is_null = false;
    result_bind[2].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[3].length = &genre_len;
    result_bind[3].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[3].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[4].buffer = nullptr; // filled below
    result_bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[4].length = &price_len;
    result_bind[4].buffer_length = 0;
    bool price_is_null = false;
    result_bind[4].is_null = &price_is_null;

    // published_at — date? (two-phase)
    result_bind[5].buffer = nullptr; // filled below
    result_bind[5].buffer_type = MYSQL_TYPE_STRING;
    unsigned long published_at_len = 0;
    result_bind[5].length = &published_at_len;
    result_bind[5].buffer_length = 0;
    bool published_at_is_null = false;
    result_bind[5].is_null = &published_at_is_null;
    stmt.bind_result(result_bind);

    if (!stmt.fetch_row()) return std::nullopt;

    std::string title_val(title_len, '\0');
    result_bind[2].buffer = title_val.data();
    result_bind[2].buffer_length = title_len;
    stmt.fetch_column(&result_bind[2], 2);

    std::string genre_val(genre_len, '\0');
    result_bind[3].buffer = genre_val.data();
    result_bind[3].buffer_length = genre_len;
    stmt.fetch_column(&result_bind[3], 3);

    std::string price_val(price_len, '\0');
    result_bind[4].buffer = price_val.data();
    result_bind[4].buffer_length = price_len;
    stmt.fetch_column(&result_bind[4], 4);

    std::string published_at_val(published_at_len, '\0');
    result_bind[5].buffer = published_at_val.data();
    result_bind[5].buffer_length = published_at_len;
    stmt.fetch_column(&result_bind[5], 5);

    return Book{
        id_val,
        author_id_val,
        std::move(title_val),
        std::move(genre_val),
        std::move(price_val),
        published_at_is_null ? std::nullopt : std::optional<std::string>(std::move(published_at_val))
    };
}

std::vector<Book> get_books_by_ids(MYSQL* db, const std::vector<std::int64_t>& ids) {
    MysqlStmt stmt(db, SQL_GET_BOOKS_BY_IDS);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // ids — bigint[] (JSON)
    std::string p_ids_json = "[";
    for (size_t i = 0; i < ids.size(); ++i) {
        if (i > 0) p_ids_json += ",";
        p_ids_json += std::to_string(ids[i]);
    }
    p_ids_json += "]";
    bind[0].buffer = const_cast<char*>(p_ids_json.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_ids_json_len = p_ids_json.size();
    bind[0].length = &p_ids_json_len;
    bind[0].buffer_length = p_ids_json_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[6];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // author_id — bigint
    std::int64_t author_id_val{};
    result_bind[1].buffer = &author_id_val;
    result_bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    bool author_id_is_null = false;
    result_bind[1].is_null = &author_id_is_null;

    // title — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[2].length = &title_len;
    result_bind[2].buffer_length = 0;
    bool title_is_null = false;
    result_bind[2].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[3].length = &genre_len;
    result_bind[3].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[3].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[4].buffer = nullptr; // filled below
    result_bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[4].length = &price_len;
    result_bind[4].buffer_length = 0;
    bool price_is_null = false;
    result_bind[4].is_null = &price_is_null;

    // published_at — date? (two-phase)
    result_bind[5].buffer = nullptr; // filled below
    result_bind[5].buffer_type = MYSQL_TYPE_STRING;
    unsigned long published_at_len = 0;
    result_bind[5].length = &published_at_len;
    result_bind[5].buffer_length = 0;
    bool published_at_is_null = false;
    result_bind[5].is_null = &published_at_is_null;
    stmt.bind_result(result_bind);

    std::vector<Book> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[2].buffer = title_val.data();
        result_bind[2].buffer_length = title_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string genre_val(genre_len, '\0');
        result_bind[3].buffer = genre_val.data();
        result_bind[3].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[3], 3);

        std::string price_val(price_len, '\0');
        result_bind[4].buffer = price_val.data();
        result_bind[4].buffer_length = price_len;
        stmt.fetch_column(&result_bind[4], 4);

        std::string published_at_val(published_at_len, '\0');
        result_bind[5].buffer = published_at_val.data();
        result_bind[5].buffer_length = published_at_len;
        stmt.fetch_column(&result_bind[5], 5);

        rows.push_back(Book{
            id_val,
            author_id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val),
            published_at_is_null ? std::nullopt : std::optional<std::string>(std::move(published_at_val))
        });
    }
    return rows;
}

std::vector<Book> list_books_by_genre(MYSQL* db, const std::string& genre) {
    MysqlStmt stmt(db, SQL_LIST_BOOKS_BY_GENRE);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // genre — string
    bind[0].buffer = const_cast<char*>(genre.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_genre_len = genre.size();
    bind[0].length = &p_genre_len;
    bind[0].buffer_length = p_genre_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[6];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // author_id — bigint
    std::int64_t author_id_val{};
    result_bind[1].buffer = &author_id_val;
    result_bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    bool author_id_is_null = false;
    result_bind[1].is_null = &author_id_is_null;

    // title — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[2].length = &title_len;
    result_bind[2].buffer_length = 0;
    bool title_is_null = false;
    result_bind[2].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[3].length = &genre_len;
    result_bind[3].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[3].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[4].buffer = nullptr; // filled below
    result_bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[4].length = &price_len;
    result_bind[4].buffer_length = 0;
    bool price_is_null = false;
    result_bind[4].is_null = &price_is_null;

    // published_at — date? (two-phase)
    result_bind[5].buffer = nullptr; // filled below
    result_bind[5].buffer_type = MYSQL_TYPE_STRING;
    unsigned long published_at_len = 0;
    result_bind[5].length = &published_at_len;
    result_bind[5].buffer_length = 0;
    bool published_at_is_null = false;
    result_bind[5].is_null = &published_at_is_null;
    stmt.bind_result(result_bind);

    std::vector<Book> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[2].buffer = title_val.data();
        result_bind[2].buffer_length = title_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string genre_val(genre_len, '\0');
        result_bind[3].buffer = genre_val.data();
        result_bind[3].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[3], 3);

        std::string price_val(price_len, '\0');
        result_bind[4].buffer = price_val.data();
        result_bind[4].buffer_length = price_len;
        stmt.fetch_column(&result_bind[4], 4);

        std::string published_at_val(published_at_len, '\0');
        result_bind[5].buffer = published_at_val.data();
        result_bind[5].buffer_length = published_at_len;
        stmt.fetch_column(&result_bind[5], 5);

        rows.push_back(Book{
            id_val,
            author_id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val),
            published_at_is_null ? std::nullopt : std::optional<std::string>(std::move(published_at_val))
        });
    }
    return rows;
}

std::vector<Book> list_books_by_genre_or_all(MYSQL* db, const std::string& genre) {
    MysqlStmt stmt(db, SQL_LIST_BOOKS_BY_GENRE_OR_ALL);

    MYSQL_BIND bind[2];
    memset(bind, 0, sizeof(bind));

    // genre — string
    bind[0].buffer = const_cast<char*>(genre.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_genre_len = genre.size();
    bind[0].length = &p_genre_len;
    bind[0].buffer_length = p_genre_len;

    // genre — string
    bind[1].buffer = const_cast<char*>(genre.c_str());
    bind[1].buffer_type = MYSQL_TYPE_STRING;
    bind[1].length = &p_genre_len;
    bind[1].buffer_length = p_genre_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[6];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // author_id — bigint
    std::int64_t author_id_val{};
    result_bind[1].buffer = &author_id_val;
    result_bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    bool author_id_is_null = false;
    result_bind[1].is_null = &author_id_is_null;

    // title — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[2].length = &title_len;
    result_bind[2].buffer_length = 0;
    bool title_is_null = false;
    result_bind[2].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[3].length = &genre_len;
    result_bind[3].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[3].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[4].buffer = nullptr; // filled below
    result_bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[4].length = &price_len;
    result_bind[4].buffer_length = 0;
    bool price_is_null = false;
    result_bind[4].is_null = &price_is_null;

    // published_at — date? (two-phase)
    result_bind[5].buffer = nullptr; // filled below
    result_bind[5].buffer_type = MYSQL_TYPE_STRING;
    unsigned long published_at_len = 0;
    result_bind[5].length = &published_at_len;
    result_bind[5].buffer_length = 0;
    bool published_at_is_null = false;
    result_bind[5].is_null = &published_at_is_null;
    stmt.bind_result(result_bind);

    std::vector<Book> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[2].buffer = title_val.data();
        result_bind[2].buffer_length = title_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string genre_val(genre_len, '\0');
        result_bind[3].buffer = genre_val.data();
        result_bind[3].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[3], 3);

        std::string price_val(price_len, '\0');
        result_bind[4].buffer = price_val.data();
        result_bind[4].buffer_length = price_len;
        stmt.fetch_column(&result_bind[4], 4);

        std::string published_at_val(published_at_len, '\0');
        result_bind[5].buffer = published_at_val.data();
        result_bind[5].buffer_length = published_at_len;
        stmt.fetch_column(&result_bind[5], 5);

        rows.push_back(Book{
            id_val,
            author_id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val),
            published_at_is_null ? std::nullopt : std::optional<std::string>(std::move(published_at_val))
        });
    }
    return rows;
}

void create_customer(MYSQL* db, const std::string& name, const std::string& email) {
    MysqlStmt stmt(db, SQL_CREATE_CUSTOMER);

    MYSQL_BIND bind[2];
    memset(bind, 0, sizeof(bind));

    // name — string
    bind[0].buffer = const_cast<char*>(name.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_name_len = name.size();
    bind[0].length = &p_name_len;
    bind[0].buffer_length = p_name_len;

    // email — string
    bind[1].buffer = const_cast<char*>(email.c_str());
    bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_email_len = email.size();
    bind[1].length = &p_email_len;
    bind[1].buffer_length = p_email_len;
    stmt.bind_param(bind);

    stmt.execute();
}

void create_sale(MYSQL* db, std::int64_t customer_id) {
    MysqlStmt stmt(db, SQL_CREATE_SALE);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // customer_id — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&customer_id);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    stmt.bind_param(bind);

    stmt.execute();
}

void add_sale_item(MYSQL* db, std::int64_t sale_id, std::int64_t book_id, std::int32_t quantity, const std::string& unit_price) {
    MysqlStmt stmt(db, SQL_ADD_SALE_ITEM);

    MYSQL_BIND bind[4];
    memset(bind, 0, sizeof(bind));

    // sale_id — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&sale_id);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;

    // book_id — bigint
    bind[1].buffer = const_cast<std::int64_t*>(&book_id);
    bind[1].buffer_type = MYSQL_TYPE_LONGLONG;

    // quantity — int
    bind[2].buffer = const_cast<std::int32_t*>(&quantity);
    bind[2].buffer_type = MYSQL_TYPE_LONG;

    // unit_price — decimal
    bind[3].buffer = const_cast<char*>(unit_price.c_str());
    bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_unit_price_len = unit_price.size();
    bind[3].length = &p_unit_price_len;
    bind[3].buffer_length = p_unit_price_len;
    stmt.bind_param(bind);

    stmt.execute();
}

std::vector<ListBooksWithAuthorRow> list_books_with_author(MYSQL* db) {
    MysqlStmt stmt(db, SQL_LIST_BOOKS_WITH_AUTHOR);
    stmt.execute();

    MYSQL_BIND result_bind[7];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[3].length = &price_len;
    result_bind[3].buffer_length = 0;
    bool price_is_null = false;
    result_bind[3].is_null = &price_is_null;

    // published_at — date? (two-phase)
    result_bind[4].buffer = nullptr; // filled below
    result_bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long published_at_len = 0;
    result_bind[4].length = &published_at_len;
    result_bind[4].buffer_length = 0;
    bool published_at_is_null = false;
    result_bind[4].is_null = &published_at_is_null;

    // author_name — string (two-phase)
    result_bind[5].buffer = nullptr; // filled below
    result_bind[5].buffer_type = MYSQL_TYPE_STRING;
    unsigned long author_name_len = 0;
    result_bind[5].length = &author_name_len;
    result_bind[5].buffer_length = 0;
    bool author_name_is_null = false;
    result_bind[5].is_null = &author_name_is_null;

    // author_bio — string? (two-phase)
    result_bind[6].buffer = nullptr; // filled below
    result_bind[6].buffer_type = MYSQL_TYPE_STRING;
    unsigned long author_bio_len = 0;
    result_bind[6].length = &author_bio_len;
    result_bind[6].buffer_length = 0;
    bool author_bio_is_null = false;
    result_bind[6].is_null = &author_bio_is_null;
    stmt.bind_result(result_bind);

    std::vector<ListBooksWithAuthorRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string price_val(price_len, '\0');
        result_bind[3].buffer = price_val.data();
        result_bind[3].buffer_length = price_len;
        stmt.fetch_column(&result_bind[3], 3);

        std::string published_at_val(published_at_len, '\0');
        result_bind[4].buffer = published_at_val.data();
        result_bind[4].buffer_length = published_at_len;
        stmt.fetch_column(&result_bind[4], 4);

        std::string author_name_val(author_name_len, '\0');
        result_bind[5].buffer = author_name_val.data();
        result_bind[5].buffer_length = author_name_len;
        stmt.fetch_column(&result_bind[5], 5);

        std::string author_bio_val(author_bio_len, '\0');
        result_bind[6].buffer = author_bio_val.data();
        result_bind[6].buffer_length = author_bio_len;
        stmt.fetch_column(&result_bind[6], 6);

        rows.push_back(ListBooksWithAuthorRow{
            id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val),
            published_at_is_null ? std::nullopt : std::optional<std::string>(std::move(published_at_val)),
            std::move(author_name_val),
            author_bio_is_null ? std::nullopt : std::optional<std::string>(std::move(author_bio_val))
        });
    }
    return rows;
}

std::vector<Book> get_books_never_ordered(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_BOOKS_NEVER_ORDERED);
    stmt.execute();

    MYSQL_BIND result_bind[6];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // author_id — bigint
    std::int64_t author_id_val{};
    result_bind[1].buffer = &author_id_val;
    result_bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    bool author_id_is_null = false;
    result_bind[1].is_null = &author_id_is_null;

    // title — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[2].length = &title_len;
    result_bind[2].buffer_length = 0;
    bool title_is_null = false;
    result_bind[2].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[3].length = &genre_len;
    result_bind[3].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[3].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[4].buffer = nullptr; // filled below
    result_bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[4].length = &price_len;
    result_bind[4].buffer_length = 0;
    bool price_is_null = false;
    result_bind[4].is_null = &price_is_null;

    // published_at — date? (two-phase)
    result_bind[5].buffer = nullptr; // filled below
    result_bind[5].buffer_type = MYSQL_TYPE_STRING;
    unsigned long published_at_len = 0;
    result_bind[5].length = &published_at_len;
    result_bind[5].buffer_length = 0;
    bool published_at_is_null = false;
    result_bind[5].is_null = &published_at_is_null;
    stmt.bind_result(result_bind);

    std::vector<Book> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[2].buffer = title_val.data();
        result_bind[2].buffer_length = title_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string genre_val(genre_len, '\0');
        result_bind[3].buffer = genre_val.data();
        result_bind[3].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[3], 3);

        std::string price_val(price_len, '\0');
        result_bind[4].buffer = price_val.data();
        result_bind[4].buffer_length = price_len;
        stmt.fetch_column(&result_bind[4], 4);

        std::string published_at_val(published_at_len, '\0');
        result_bind[5].buffer = published_at_val.data();
        result_bind[5].buffer_length = published_at_len;
        stmt.fetch_column(&result_bind[5], 5);

        rows.push_back(Book{
            id_val,
            author_id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val),
            published_at_is_null ? std::nullopt : std::optional<std::string>(std::move(published_at_val))
        });
    }
    return rows;
}

std::vector<GetTopSellingBooksRow> get_top_selling_books(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_TOP_SELLING_BOOKS);
    stmt.execute();

    MYSQL_BIND result_bind[5];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[3].length = &price_len;
    result_bind[3].buffer_length = 0;
    bool price_is_null = false;
    result_bind[3].is_null = &price_is_null;

    // units_sold — decimal? (two-phase)
    result_bind[4].buffer = nullptr; // filled below
    result_bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long units_sold_len = 0;
    result_bind[4].length = &units_sold_len;
    result_bind[4].buffer_length = 0;
    bool units_sold_is_null = false;
    result_bind[4].is_null = &units_sold_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetTopSellingBooksRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string price_val(price_len, '\0');
        result_bind[3].buffer = price_val.data();
        result_bind[3].buffer_length = price_len;
        stmt.fetch_column(&result_bind[3], 3);

        std::string units_sold_val(units_sold_len, '\0');
        result_bind[4].buffer = units_sold_val.data();
        result_bind[4].buffer_length = units_sold_len;
        stmt.fetch_column(&result_bind[4], 4);

        rows.push_back(GetTopSellingBooksRow{
            id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val),
            units_sold_is_null ? std::nullopt : std::optional<std::string>(std::move(units_sold_val))
        });
    }
    return rows;
}

std::vector<GetBestCustomersRow> get_best_customers(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_BEST_CUSTOMERS);
    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // name — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long name_len = 0;
    result_bind[1].length = &name_len;
    result_bind[1].buffer_length = 0;
    bool name_is_null = false;
    result_bind[1].is_null = &name_is_null;

    // email — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long email_len = 0;
    result_bind[2].length = &email_len;
    result_bind[2].buffer_length = 0;
    bool email_is_null = false;
    result_bind[2].is_null = &email_is_null;

    // total_spent — decimal? (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long total_spent_len = 0;
    result_bind[3].length = &total_spent_len;
    result_bind[3].buffer_length = 0;
    bool total_spent_is_null = false;
    result_bind[3].is_null = &total_spent_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBestCustomersRow> rows;
    while (stmt.fetch_row()) {
        std::string name_val(name_len, '\0');
        result_bind[1].buffer = name_val.data();
        result_bind[1].buffer_length = name_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string email_val(email_len, '\0');
        result_bind[2].buffer = email_val.data();
        result_bind[2].buffer_length = email_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string total_spent_val(total_spent_len, '\0');
        result_bind[3].buffer = total_spent_val.data();
        result_bind[3].buffer_length = total_spent_len;
        stmt.fetch_column(&result_bind[3], 3);

        rows.push_back(GetBestCustomersRow{
            id_val,
            std::move(name_val),
            std::move(email_val),
            total_spent_is_null ? std::nullopt : std::optional<std::string>(std::move(total_spent_val))
        });
    }
    return rows;
}

std::vector<CountBooksByGenreRow> count_books_by_genre(MYSQL* db) {
    MysqlStmt stmt(db, SQL_COUNT_BOOKS_BY_GENRE);
    stmt.execute();

    MYSQL_BIND result_bind[2];
    memset(result_bind, 0, sizeof(result_bind));

    // genre — string (two-phase)
    result_bind[0].buffer = nullptr; // filled below
    result_bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[0].length = &genre_len;
    result_bind[0].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[0].is_null = &genre_is_null;

    // book_count — bigint
    std::int64_t book_count_val{};
    result_bind[1].buffer = &book_count_val;
    result_bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    bool book_count_is_null = false;
    result_bind[1].is_null = &book_count_is_null;
    stmt.bind_result(result_bind);

    std::vector<CountBooksByGenreRow> rows;
    while (stmt.fetch_row()) {
        std::string genre_val(genre_len, '\0');
        result_bind[0].buffer = genre_val.data();
        result_bind[0].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[0], 0);

        rows.push_back(CountBooksByGenreRow{
            std::move(genre_val),
            book_count_val
        });
    }
    return rows;
}

std::vector<ListBooksWithLimitRow> list_books_with_limit(MYSQL* db, std::int64_t limit, std::int64_t offset) {
    MysqlStmt stmt(db, SQL_LIST_BOOKS_WITH_LIMIT);

    MYSQL_BIND bind[2];
    memset(bind, 0, sizeof(bind));

    // limit — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&limit);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;

    // offset — bigint
    bind[1].buffer = const_cast<std::int64_t*>(&offset);
    bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[3].length = &price_len;
    result_bind[3].buffer_length = 0;
    bool price_is_null = false;
    result_bind[3].is_null = &price_is_null;
    stmt.bind_result(result_bind);

    std::vector<ListBooksWithLimitRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string price_val(price_len, '\0');
        result_bind[3].buffer = price_val.data();
        result_bind[3].buffer_length = price_len;
        stmt.fetch_column(&result_bind[3], 3);

        rows.push_back(ListBooksWithLimitRow{
            id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val)
        });
    }
    return rows;
}

std::vector<SearchBooksByTitleRow> search_books_by_title(MYSQL* db, const std::string& title) {
    MysqlStmt stmt(db, SQL_SEARCH_BOOKS_BY_TITLE);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // title — string
    bind[0].buffer = const_cast<char*>(title.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_title_len = title.size();
    bind[0].length = &p_title_len;
    bind[0].buffer_length = p_title_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[3].length = &price_len;
    result_bind[3].buffer_length = 0;
    bool price_is_null = false;
    result_bind[3].is_null = &price_is_null;
    stmt.bind_result(result_bind);

    std::vector<SearchBooksByTitleRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string price_val(price_len, '\0');
        result_bind[3].buffer = price_val.data();
        result_bind[3].buffer_length = price_len;
        stmt.fetch_column(&result_bind[3], 3);

        rows.push_back(SearchBooksByTitleRow{
            id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val)
        });
    }
    return rows;
}

std::vector<GetBooksByPriceRangeRow> get_books_by_price_range(MYSQL* db, const std::string& price, const std::string& price_2) {
    MysqlStmt stmt(db, SQL_GET_BOOKS_BY_PRICE_RANGE);

    MYSQL_BIND bind[2];
    memset(bind, 0, sizeof(bind));

    // price — decimal
    bind[0].buffer = const_cast<char*>(price.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_price_len = price.size();
    bind[0].length = &p_price_len;
    bind[0].buffer_length = p_price_len;

    // price_2 — decimal
    bind[1].buffer = const_cast<char*>(price_2.c_str());
    bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_price_2_len = price_2.size();
    bind[1].length = &p_price_2_len;
    bind[1].buffer_length = p_price_2_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[3].length = &price_len;
    result_bind[3].buffer_length = 0;
    bool price_is_null = false;
    result_bind[3].is_null = &price_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBooksByPriceRangeRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string price_val(price_len, '\0');
        result_bind[3].buffer = price_val.data();
        result_bind[3].buffer_length = price_len;
        stmt.fetch_column(&result_bind[3], 3);

        rows.push_back(GetBooksByPriceRangeRow{
            id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val)
        });
    }
    return rows;
}

std::vector<GetBooksInGenresRow> get_books_in_genres(MYSQL* db, const std::string& genre, const std::string& genre_2, const std::string& genre_3) {
    MysqlStmt stmt(db, SQL_GET_BOOKS_IN_GENRES);

    MYSQL_BIND bind[3];
    memset(bind, 0, sizeof(bind));

    // genre — string
    bind[0].buffer = const_cast<char*>(genre.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_genre_len = genre.size();
    bind[0].length = &p_genre_len;
    bind[0].buffer_length = p_genre_len;

    // genre_2 — string
    bind[1].buffer = const_cast<char*>(genre_2.c_str());
    bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_genre_2_len = genre_2.size();
    bind[1].length = &p_genre_2_len;
    bind[1].buffer_length = p_genre_2_len;

    // genre_3 — string
    bind[2].buffer = const_cast<char*>(genre_3.c_str());
    bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_genre_3_len = genre_3.size();
    bind[2].length = &p_genre_3_len;
    bind[2].buffer_length = p_genre_3_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[3].length = &price_len;
    result_bind[3].buffer_length = 0;
    bool price_is_null = false;
    result_bind[3].is_null = &price_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBooksInGenresRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string price_val(price_len, '\0');
        result_bind[3].buffer = price_val.data();
        result_bind[3].buffer_length = price_len;
        stmt.fetch_column(&result_bind[3], 3);

        rows.push_back(GetBooksInGenresRow{
            id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val)
        });
    }
    return rows;
}

std::vector<GetBookPriceLabelRow> get_book_price_label(MYSQL* db, const std::string& price) {
    MysqlStmt stmt(db, SQL_GET_BOOK_PRICE_LABEL);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // price — decimal
    bind[0].buffer = const_cast<char*>(price.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_price_len = price.size();
    bind[0].length = &p_price_len;
    bind[0].buffer_length = p_price_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // price — decimal (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[2].length = &price_len;
    result_bind[2].buffer_length = 0;
    bool price_is_null = false;
    result_bind[2].is_null = &price_is_null;

    // price_label — string (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_label_len = 0;
    result_bind[3].length = &price_label_len;
    result_bind[3].buffer_length = 0;
    bool price_label_is_null = false;
    result_bind[3].is_null = &price_label_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBookPriceLabelRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string price_val(price_len, '\0');
        result_bind[2].buffer = price_val.data();
        result_bind[2].buffer_length = price_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string price_label_val(price_label_len, '\0');
        result_bind[3].buffer = price_label_val.data();
        result_bind[3].buffer_length = price_label_len;
        stmt.fetch_column(&result_bind[3], 3);

        rows.push_back(GetBookPriceLabelRow{
            id_val,
            std::move(title_val),
            std::move(price_val),
            std::move(price_label_val)
        });
    }
    return rows;
}

std::vector<GetBookPriceOrDefaultRow> get_book_price_or_default(MYSQL* db, const std::optional<std::string>& price) {
    MysqlStmt stmt(db, SQL_GET_BOOK_PRICE_OR_DEFAULT);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // price — decimal?
    bind[0].buffer = const_cast<char*>(price.has_value() ? price.value().c_str() : "");
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_price_len = price.has_value() ? price.value().size() : 0;
    bind[0].length = &p_price_len;
    bind[0].buffer_length = p_price_len;
    bool p_price_is_null = !price.has_value();
    bind[0].is_null = &p_price_is_null;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[3];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // effective_price — decimal (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long effective_price_len = 0;
    result_bind[2].length = &effective_price_len;
    result_bind[2].buffer_length = 0;
    bool effective_price_is_null = false;
    result_bind[2].is_null = &effective_price_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBookPriceOrDefaultRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string effective_price_val(effective_price_len, '\0');
        result_bind[2].buffer = effective_price_val.data();
        result_bind[2].buffer_length = effective_price_len;
        stmt.fetch_column(&result_bind[2], 2);

        rows.push_back(GetBookPriceOrDefaultRow{
            id_val,
            std::move(title_val),
            std::move(effective_price_val)
        });
    }
    return rows;
}

std::int64_t delete_book_by_id(MYSQL* db, std::int64_t id) {
    MysqlStmt stmt(db, SQL_DELETE_BOOK_BY_ID);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // id — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&id);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    stmt.bind_param(bind);

    stmt.execute();
    return static_cast<std::int64_t>(stmt.affected_rows());
}

std::vector<GetGenresWithManyBooksRow> get_genres_with_many_books(MYSQL* db, std::int64_t count) {
    MysqlStmt stmt(db, SQL_GET_GENRES_WITH_MANY_BOOKS);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // count — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&count);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[2];
    memset(result_bind, 0, sizeof(result_bind));

    // genre — string (two-phase)
    result_bind[0].buffer = nullptr; // filled below
    result_bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[0].length = &genre_len;
    result_bind[0].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[0].is_null = &genre_is_null;

    // book_count — bigint
    std::int64_t book_count_val{};
    result_bind[1].buffer = &book_count_val;
    result_bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    bool book_count_is_null = false;
    result_bind[1].is_null = &book_count_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetGenresWithManyBooksRow> rows;
    while (stmt.fetch_row()) {
        std::string genre_val(genre_len, '\0');
        result_bind[0].buffer = genre_val.data();
        result_bind[0].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[0], 0);

        rows.push_back(GetGenresWithManyBooksRow{
            std::move(genre_val),
            book_count_val
        });
    }
    return rows;
}

std::vector<GetBooksByAuthorParamRow> get_books_by_author_param(MYSQL* db, const std::optional<std::int32_t>& birth_year) {
    MysqlStmt stmt(db, SQL_GET_BOOKS_BY_AUTHOR_PARAM);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // birth_year — int?
    std::int32_t birth_year_val = birth_year.value_or(std::int32_t{});
    bind[0].buffer = &birth_year_val;
    bind[0].buffer_type = MYSQL_TYPE_LONG;
    bool p_birth_year_is_null = !birth_year.has_value();
    bind[0].is_null = &p_birth_year_is_null;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[3];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // price — decimal (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[2].length = &price_len;
    result_bind[2].buffer_length = 0;
    bool price_is_null = false;
    result_bind[2].is_null = &price_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBooksByAuthorParamRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string price_val(price_len, '\0');
        result_bind[2].buffer = price_val.data();
        result_bind[2].buffer_length = price_len;
        stmt.fetch_column(&result_bind[2], 2);

        rows.push_back(GetBooksByAuthorParamRow{
            id_val,
            std::move(title_val),
            std::move(price_val)
        });
    }
    return rows;
}

std::vector<Book> get_all_book_fields(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_ALL_BOOK_FIELDS);
    stmt.execute();

    MYSQL_BIND result_bind[6];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // author_id — bigint
    std::int64_t author_id_val{};
    result_bind[1].buffer = &author_id_val;
    result_bind[1].buffer_type = MYSQL_TYPE_LONGLONG;
    bool author_id_is_null = false;
    result_bind[1].is_null = &author_id_is_null;

    // title — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[2].length = &title_len;
    result_bind[2].buffer_length = 0;
    bool title_is_null = false;
    result_bind[2].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[3].length = &genre_len;
    result_bind[3].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[3].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[4].buffer = nullptr; // filled below
    result_bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[4].length = &price_len;
    result_bind[4].buffer_length = 0;
    bool price_is_null = false;
    result_bind[4].is_null = &price_is_null;

    // published_at — date? (two-phase)
    result_bind[5].buffer = nullptr; // filled below
    result_bind[5].buffer_type = MYSQL_TYPE_STRING;
    unsigned long published_at_len = 0;
    result_bind[5].length = &published_at_len;
    result_bind[5].buffer_length = 0;
    bool published_at_is_null = false;
    result_bind[5].is_null = &published_at_is_null;
    stmt.bind_result(result_bind);

    std::vector<Book> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[2].buffer = title_val.data();
        result_bind[2].buffer_length = title_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string genre_val(genre_len, '\0');
        result_bind[3].buffer = genre_val.data();
        result_bind[3].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[3], 3);

        std::string price_val(price_len, '\0');
        result_bind[4].buffer = price_val.data();
        result_bind[4].buffer_length = price_len;
        stmt.fetch_column(&result_bind[4], 4);

        std::string published_at_val(published_at_len, '\0');
        result_bind[5].buffer = published_at_val.data();
        result_bind[5].buffer_length = published_at_len;
        stmt.fetch_column(&result_bind[5], 5);

        rows.push_back(Book{
            id_val,
            author_id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val),
            published_at_is_null ? std::nullopt : std::optional<std::string>(std::move(published_at_val))
        });
    }
    return rows;
}

std::vector<GetBooksNotByAuthorRow> get_books_not_by_author(MYSQL* db, const std::string& name) {
    MysqlStmt stmt(db, SQL_GET_BOOKS_NOT_BY_AUTHOR);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // name — string
    bind[0].buffer = const_cast<char*>(name.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_name_len = name.size();
    bind[0].length = &p_name_len;
    bind[0].buffer_length = p_name_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[3];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBooksNotByAuthorRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        rows.push_back(GetBooksNotByAuthorRow{
            id_val,
            std::move(title_val),
            std::move(genre_val)
        });
    }
    return rows;
}

std::vector<GetBooksWithRecentSalesRow> get_books_with_recent_sales(MYSQL* db, const std::string& ordered_at) {
    MysqlStmt stmt(db, SQL_GET_BOOKS_WITH_RECENT_SALES);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // ordered_at — timestamp
    bind[0].buffer = const_cast<char*>(ordered_at.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_ordered_at_len = ordered_at.size();
    bind[0].length = &p_ordered_at_len;
    bind[0].buffer_length = p_ordered_at_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[3];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBooksWithRecentSalesRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        rows.push_back(GetBooksWithRecentSalesRow{
            id_val,
            std::move(title_val),
            std::move(genre_val)
        });
    }
    return rows;
}

std::vector<GetBookWithAuthorNameRow> get_book_with_author_name(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_BOOK_WITH_AUTHOR_NAME);
    stmt.execute();

    MYSQL_BIND result_bind[3];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // author_name — string? (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long author_name_len = 0;
    result_bind[2].length = &author_name_len;
    result_bind[2].buffer_length = 0;
    bool author_name_is_null = false;
    result_bind[2].is_null = &author_name_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBookWithAuthorNameRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string author_name_val(author_name_len, '\0');
        result_bind[2].buffer = author_name_val.data();
        result_bind[2].buffer_length = author_name_len;
        stmt.fetch_column(&result_bind[2], 2);

        rows.push_back(GetBookWithAuthorNameRow{
            id_val,
            std::move(title_val),
            author_name_is_null ? std::nullopt : std::optional<std::string>(std::move(author_name_val))
        });
    }
    return rows;
}

std::vector<GetAuthorStatsRow> get_author_stats(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_AUTHOR_STATS);
    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // name — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long name_len = 0;
    result_bind[1].length = &name_len;
    result_bind[1].buffer_length = 0;
    bool name_is_null = false;
    result_bind[1].is_null = &name_is_null;

    // num_books — bigint
    std::int64_t num_books_val{};
    result_bind[2].buffer = &num_books_val;
    result_bind[2].buffer_type = MYSQL_TYPE_LONGLONG;
    bool num_books_is_null = false;
    result_bind[2].is_null = &num_books_is_null;

    // total_sold — decimal (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long total_sold_len = 0;
    result_bind[3].length = &total_sold_len;
    result_bind[3].buffer_length = 0;
    bool total_sold_is_null = false;
    result_bind[3].is_null = &total_sold_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetAuthorStatsRow> rows;
    while (stmt.fetch_row()) {
        std::string name_val(name_len, '\0');
        result_bind[1].buffer = name_val.data();
        result_bind[1].buffer_length = name_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string total_sold_val(total_sold_len, '\0');
        result_bind[3].buffer = total_sold_val.data();
        result_bind[3].buffer_length = total_sold_len;
        stmt.fetch_column(&result_bind[3], 3);

        rows.push_back(GetAuthorStatsRow{
            id_val,
            std::move(name_val),
            num_books_val,
            std::move(total_sold_val)
        });
    }
    return rows;
}

std::optional<Product> get_product(MYSQL* db, const std::string& id) {
    MysqlStmt stmt(db, SQL_GET_PRODUCT);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // id — string
    bind[0].buffer = const_cast<char*>(id.c_str());
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_id_len = id.size();
    bind[0].length = &p_id_len;
    bind[0].buffer_length = p_id_len;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[10];
    memset(result_bind, 0, sizeof(result_bind));

    // id — string (two-phase)
    result_bind[0].buffer = nullptr; // filled below
    result_bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long id_len = 0;
    result_bind[0].length = &id_len;
    result_bind[0].buffer_length = 0;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // sku — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long sku_len = 0;
    result_bind[1].length = &sku_len;
    result_bind[1].buffer_length = 0;
    bool sku_is_null = false;
    result_bind[1].is_null = &sku_is_null;

    // name — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long name_len = 0;
    result_bind[2].length = &name_len;
    result_bind[2].buffer_length = 0;
    bool name_is_null = false;
    result_bind[2].is_null = &name_is_null;

    // active — bool
    bool active_val{};
    result_bind[3].buffer = &active_val;
    result_bind[3].buffer_type = MYSQL_TYPE_TINY;
    bool active_is_null = false;
    result_bind[3].is_null = &active_is_null;

    // weight_kg — float?
    float weight_kg_val{};
    result_bind[4].buffer = &weight_kg_val;
    result_bind[4].buffer_type = MYSQL_TYPE_FLOAT;
    bool weight_kg_is_null = false;
    result_bind[4].is_null = &weight_kg_is_null;

    // rating — double?
    double rating_val{};
    result_bind[5].buffer = &rating_val;
    result_bind[5].buffer_type = MYSQL_TYPE_DOUBLE;
    bool rating_is_null = false;
    result_bind[5].is_null = &rating_is_null;

    // metadata — json? (two-phase)
    result_bind[6].buffer = nullptr; // filled below
    result_bind[6].buffer_type = MYSQL_TYPE_STRING;
    unsigned long metadata_len = 0;
    result_bind[6].length = &metadata_len;
    result_bind[6].buffer_length = 0;
    bool metadata_is_null = false;
    result_bind[6].is_null = &metadata_is_null;

    // thumbnail — bytes? (two-phase)
    result_bind[7].buffer = nullptr; // filled below
    result_bind[7].buffer_type = MYSQL_TYPE_BLOB;
    unsigned long thumbnail_len = 0;
    result_bind[7].length = &thumbnail_len;
    result_bind[7].buffer_length = 0;
    bool thumbnail_is_null = false;
    result_bind[7].is_null = &thumbnail_is_null;

    // created_at — timestamp (two-phase)
    result_bind[8].buffer = nullptr; // filled below
    result_bind[8].buffer_type = MYSQL_TYPE_STRING;
    unsigned long created_at_len = 0;
    result_bind[8].length = &created_at_len;
    result_bind[8].buffer_length = 0;
    bool created_at_is_null = false;
    result_bind[8].is_null = &created_at_is_null;

    // stock_count — smallint
    std::int16_t stock_count_val{};
    result_bind[9].buffer = &stock_count_val;
    result_bind[9].buffer_type = MYSQL_TYPE_SHORT;
    bool stock_count_is_null = false;
    result_bind[9].is_null = &stock_count_is_null;
    stmt.bind_result(result_bind);

    if (!stmt.fetch_row()) return std::nullopt;

    std::string id_val(id_len, '\0');
    result_bind[0].buffer = id_val.data();
    result_bind[0].buffer_length = id_len;
    stmt.fetch_column(&result_bind[0], 0);

    std::string sku_val(sku_len, '\0');
    result_bind[1].buffer = sku_val.data();
    result_bind[1].buffer_length = sku_len;
    stmt.fetch_column(&result_bind[1], 1);

    std::string name_val(name_len, '\0');
    result_bind[2].buffer = name_val.data();
    result_bind[2].buffer_length = name_len;
    stmt.fetch_column(&result_bind[2], 2);

    std::string metadata_val(metadata_len, '\0');
    result_bind[6].buffer = metadata_val.data();
    result_bind[6].buffer_length = metadata_len;
    stmt.fetch_column(&result_bind[6], 6);

    std::vector<std::uint8_t> thumbnail_val(thumbnail_len);
    result_bind[7].buffer = thumbnail_val.data();
    result_bind[7].buffer_length = thumbnail_len;
    stmt.fetch_column(&result_bind[7], 7);

    std::string created_at_val(created_at_len, '\0');
    result_bind[8].buffer = created_at_val.data();
    result_bind[8].buffer_length = created_at_len;
    stmt.fetch_column(&result_bind[8], 8);

    return Product{
        std::move(id_val),
        std::move(sku_val),
        std::move(name_val),
        active_val,
        weight_kg_is_null ? std::nullopt : std::optional<float>(weight_kg_val),
        rating_is_null ? std::nullopt : std::optional<double>(rating_val),
        metadata_is_null ? std::nullopt : std::optional<std::string>(std::move(metadata_val)),
        thumbnail_is_null ? std::nullopt : std::optional<std::vector<std::uint8_t>>(std::move(thumbnail_val)),
        std::move(created_at_val),
        stock_count_val
    };
}

std::vector<ListActiveProductsRow> list_active_products(MYSQL* db, bool active) {
    MysqlStmt stmt(db, SQL_LIST_ACTIVE_PRODUCTS);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // active — bool
    bind[0].buffer = const_cast<bool*>(&active);
    bind[0].buffer_type = MYSQL_TYPE_TINY;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[9];
    memset(result_bind, 0, sizeof(result_bind));

    // id — string (two-phase)
    result_bind[0].buffer = nullptr; // filled below
    result_bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long id_len = 0;
    result_bind[0].length = &id_len;
    result_bind[0].buffer_length = 0;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // sku — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long sku_len = 0;
    result_bind[1].length = &sku_len;
    result_bind[1].buffer_length = 0;
    bool sku_is_null = false;
    result_bind[1].is_null = &sku_is_null;

    // name — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long name_len = 0;
    result_bind[2].length = &name_len;
    result_bind[2].buffer_length = 0;
    bool name_is_null = false;
    result_bind[2].is_null = &name_is_null;

    // active — bool
    bool active_val{};
    result_bind[3].buffer = &active_val;
    result_bind[3].buffer_type = MYSQL_TYPE_TINY;
    bool active_is_null = false;
    result_bind[3].is_null = &active_is_null;

    // weight_kg — float?
    float weight_kg_val{};
    result_bind[4].buffer = &weight_kg_val;
    result_bind[4].buffer_type = MYSQL_TYPE_FLOAT;
    bool weight_kg_is_null = false;
    result_bind[4].is_null = &weight_kg_is_null;

    // rating — double?
    double rating_val{};
    result_bind[5].buffer = &rating_val;
    result_bind[5].buffer_type = MYSQL_TYPE_DOUBLE;
    bool rating_is_null = false;
    result_bind[5].is_null = &rating_is_null;

    // metadata — json? (two-phase)
    result_bind[6].buffer = nullptr; // filled below
    result_bind[6].buffer_type = MYSQL_TYPE_STRING;
    unsigned long metadata_len = 0;
    result_bind[6].length = &metadata_len;
    result_bind[6].buffer_length = 0;
    bool metadata_is_null = false;
    result_bind[6].is_null = &metadata_is_null;

    // created_at — timestamp (two-phase)
    result_bind[7].buffer = nullptr; // filled below
    result_bind[7].buffer_type = MYSQL_TYPE_STRING;
    unsigned long created_at_len = 0;
    result_bind[7].length = &created_at_len;
    result_bind[7].buffer_length = 0;
    bool created_at_is_null = false;
    result_bind[7].is_null = &created_at_is_null;

    // stock_count — smallint
    std::int16_t stock_count_val{};
    result_bind[8].buffer = &stock_count_val;
    result_bind[8].buffer_type = MYSQL_TYPE_SHORT;
    bool stock_count_is_null = false;
    result_bind[8].is_null = &stock_count_is_null;
    stmt.bind_result(result_bind);

    std::vector<ListActiveProductsRow> rows;
    while (stmt.fetch_row()) {
        std::string id_val(id_len, '\0');
        result_bind[0].buffer = id_val.data();
        result_bind[0].buffer_length = id_len;
        stmt.fetch_column(&result_bind[0], 0);

        std::string sku_val(sku_len, '\0');
        result_bind[1].buffer = sku_val.data();
        result_bind[1].buffer_length = sku_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string name_val(name_len, '\0');
        result_bind[2].buffer = name_val.data();
        result_bind[2].buffer_length = name_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string metadata_val(metadata_len, '\0');
        result_bind[6].buffer = metadata_val.data();
        result_bind[6].buffer_length = metadata_len;
        stmt.fetch_column(&result_bind[6], 6);

        std::string created_at_val(created_at_len, '\0');
        result_bind[7].buffer = created_at_val.data();
        result_bind[7].buffer_length = created_at_len;
        stmt.fetch_column(&result_bind[7], 7);

        rows.push_back(ListActiveProductsRow{
            std::move(id_val),
            std::move(sku_val),
            std::move(name_val),
            active_val,
            weight_kg_is_null ? std::nullopt : std::optional<float>(weight_kg_val),
            rating_is_null ? std::nullopt : std::optional<double>(rating_val),
            metadata_is_null ? std::nullopt : std::optional<std::string>(std::move(metadata_val)),
            std::move(created_at_val),
            stock_count_val
        });
    }
    return rows;
}

std::vector<GetAuthorsWithNullBioRow> get_authors_with_null_bio(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_AUTHORS_WITH_NULL_BIO);
    stmt.execute();

    MYSQL_BIND result_bind[3];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // name — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long name_len = 0;
    result_bind[1].length = &name_len;
    result_bind[1].buffer_length = 0;
    bool name_is_null = false;
    result_bind[1].is_null = &name_is_null;

    // birth_year — int?
    std::int32_t birth_year_val{};
    result_bind[2].buffer = &birth_year_val;
    result_bind[2].buffer_type = MYSQL_TYPE_LONG;
    bool birth_year_is_null = false;
    result_bind[2].is_null = &birth_year_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetAuthorsWithNullBioRow> rows;
    while (stmt.fetch_row()) {
        std::string name_val(name_len, '\0');
        result_bind[1].buffer = name_val.data();
        result_bind[1].buffer_length = name_len;
        stmt.fetch_column(&result_bind[1], 1);

        rows.push_back(GetAuthorsWithNullBioRow{
            id_val,
            std::move(name_val),
            birth_year_is_null ? std::nullopt : std::optional<std::int32_t>(birth_year_val)
        });
    }
    return rows;
}

std::vector<Author> get_authors_with_bio(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_AUTHORS_WITH_BIO);
    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // name — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long name_len = 0;
    result_bind[1].length = &name_len;
    result_bind[1].buffer_length = 0;
    bool name_is_null = false;
    result_bind[1].is_null = &name_is_null;

    // bio — string? (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long bio_len = 0;
    result_bind[2].length = &bio_len;
    result_bind[2].buffer_length = 0;
    bool bio_is_null = false;
    result_bind[2].is_null = &bio_is_null;

    // birth_year — int?
    std::int32_t birth_year_val{};
    result_bind[3].buffer = &birth_year_val;
    result_bind[3].buffer_type = MYSQL_TYPE_LONG;
    bool birth_year_is_null = false;
    result_bind[3].is_null = &birth_year_is_null;
    stmt.bind_result(result_bind);

    std::vector<Author> rows;
    while (stmt.fetch_row()) {
        std::string name_val(name_len, '\0');
        result_bind[1].buffer = name_val.data();
        result_bind[1].buffer_length = name_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string bio_val(bio_len, '\0');
        result_bind[2].buffer = bio_val.data();
        result_bind[2].buffer_length = bio_len;
        stmt.fetch_column(&result_bind[2], 2);

        rows.push_back(Author{
            id_val,
            std::move(name_val),
            bio_is_null ? std::nullopt : std::optional<std::string>(std::move(bio_val)),
            birth_year_is_null ? std::nullopt : std::optional<std::int32_t>(birth_year_val)
        });
    }
    return rows;
}

std::vector<GetBooksPublishedBetweenRow> get_books_published_between(MYSQL* db, const std::optional<std::string>& published_at, const std::optional<std::string>& published_at_2) {
    MysqlStmt stmt(db, SQL_GET_BOOKS_PUBLISHED_BETWEEN);

    MYSQL_BIND bind[2];
    memset(bind, 0, sizeof(bind));

    // published_at — date?
    bind[0].buffer = const_cast<char*>(published_at.has_value() ? published_at.value().c_str() : "");
    bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_published_at_len = published_at.has_value() ? published_at.value().size() : 0;
    bind[0].length = &p_published_at_len;
    bind[0].buffer_length = p_published_at_len;
    bool p_published_at_is_null = !published_at.has_value();
    bind[0].is_null = &p_published_at_is_null;

    // published_at_2 — date?
    bind[1].buffer = const_cast<char*>(published_at_2.has_value() ? published_at_2.value().c_str() : "");
    bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long p_published_at_2_len = published_at_2.has_value() ? published_at_2.value().size() : 0;
    bind[1].length = &p_published_at_2_len;
    bind[1].buffer_length = p_published_at_2_len;
    bool p_published_at_2_is_null = !published_at_2.has_value();
    bind[1].is_null = &p_published_at_2_is_null;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[5];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;

    // price — decimal (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long price_len = 0;
    result_bind[3].length = &price_len;
    result_bind[3].buffer_length = 0;
    bool price_is_null = false;
    result_bind[3].is_null = &price_is_null;

    // published_at — date? (two-phase)
    result_bind[4].buffer = nullptr; // filled below
    result_bind[4].buffer_type = MYSQL_TYPE_STRING;
    unsigned long published_at_len = 0;
    result_bind[4].length = &published_at_len;
    result_bind[4].buffer_length = 0;
    bool published_at_is_null = false;
    result_bind[4].is_null = &published_at_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBooksPublishedBetweenRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string price_val(price_len, '\0');
        result_bind[3].buffer = price_val.data();
        result_bind[3].buffer_length = price_len;
        stmt.fetch_column(&result_bind[3], 3);

        std::string published_at_val(published_at_len, '\0');
        result_bind[4].buffer = published_at_val.data();
        result_bind[4].buffer_length = published_at_len;
        stmt.fetch_column(&result_bind[4], 4);

        rows.push_back(GetBooksPublishedBetweenRow{
            id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(price_val),
            published_at_is_null ? std::nullopt : std::optional<std::string>(std::move(published_at_val))
        });
    }
    return rows;
}

std::vector<GetDistinctGenresRow> get_distinct_genres(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_DISTINCT_GENRES);
    stmt.execute();

    MYSQL_BIND result_bind[1];
    memset(result_bind, 0, sizeof(result_bind));

    // genre — string (two-phase)
    result_bind[0].buffer = nullptr; // filled below
    result_bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[0].length = &genre_len;
    result_bind[0].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[0].is_null = &genre_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetDistinctGenresRow> rows;
    while (stmt.fetch_row()) {
        std::string genre_val(genre_len, '\0');
        result_bind[0].buffer = genre_val.data();
        result_bind[0].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[0], 0);

        rows.push_back(GetDistinctGenresRow{
            std::move(genre_val)
        });
    }
    return rows;
}

std::vector<GetBooksWithSalesCountRow> get_books_with_sales_count(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_BOOKS_WITH_SALES_COUNT);
    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // id — bigint
    std::int64_t id_val{};
    result_bind[0].buffer = &id_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool id_is_null = false;
    result_bind[0].is_null = &id_is_null;

    // title — string (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long title_len = 0;
    result_bind[1].length = &title_len;
    result_bind[1].buffer_length = 0;
    bool title_is_null = false;
    result_bind[1].is_null = &title_is_null;

    // genre — string (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long genre_len = 0;
    result_bind[2].length = &genre_len;
    result_bind[2].buffer_length = 0;
    bool genre_is_null = false;
    result_bind[2].is_null = &genre_is_null;

    // total_quantity — decimal (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long total_quantity_len = 0;
    result_bind[3].length = &total_quantity_len;
    result_bind[3].buffer_length = 0;
    bool total_quantity_is_null = false;
    result_bind[3].is_null = &total_quantity_is_null;
    stmt.bind_result(result_bind);

    std::vector<GetBooksWithSalesCountRow> rows;
    while (stmt.fetch_row()) {
        std::string title_val(title_len, '\0');
        result_bind[1].buffer = title_val.data();
        result_bind[1].buffer_length = title_len;
        stmt.fetch_column(&result_bind[1], 1);

        std::string genre_val(genre_len, '\0');
        result_bind[2].buffer = genre_val.data();
        result_bind[2].buffer_length = genre_len;
        stmt.fetch_column(&result_bind[2], 2);

        std::string total_quantity_val(total_quantity_len, '\0');
        result_bind[3].buffer = total_quantity_val.data();
        result_bind[3].buffer_length = total_quantity_len;
        stmt.fetch_column(&result_bind[3], 3);

        rows.push_back(GetBooksWithSalesCountRow{
            id_val,
            std::move(title_val),
            std::move(genre_val),
            std::move(total_quantity_val)
        });
    }
    return rows;
}

std::optional<CountSaleItemsRow> count_sale_items(MYSQL* db, std::int64_t sale_id) {
    MysqlStmt stmt(db, SQL_COUNT_SALE_ITEMS);

    MYSQL_BIND bind[1];
    memset(bind, 0, sizeof(bind));

    // sale_id — bigint
    bind[0].buffer = const_cast<std::int64_t*>(&sale_id);
    bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    stmt.bind_param(bind);

    stmt.execute();

    MYSQL_BIND result_bind[1];
    memset(result_bind, 0, sizeof(result_bind));

    // item_count — bigint
    std::int64_t item_count_val{};
    result_bind[0].buffer = &item_count_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONGLONG;
    bool item_count_is_null = false;
    result_bind[0].is_null = &item_count_is_null;
    stmt.bind_result(result_bind);

    if (!stmt.fetch_row()) return std::nullopt;
    return CountSaleItemsRow{
        item_count_val
    };
}

std::optional<GetSaleItemQuantityAggregatesRow> get_sale_item_quantity_aggregates(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_SALE_ITEM_QUANTITY_AGGREGATES);
    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // min_qty — int?
    std::int32_t min_qty_val{};
    result_bind[0].buffer = &min_qty_val;
    result_bind[0].buffer_type = MYSQL_TYPE_LONG;
    bool min_qty_is_null = false;
    result_bind[0].is_null = &min_qty_is_null;

    // max_qty — int?
    std::int32_t max_qty_val{};
    result_bind[1].buffer = &max_qty_val;
    result_bind[1].buffer_type = MYSQL_TYPE_LONG;
    bool max_qty_is_null = false;
    result_bind[1].is_null = &max_qty_is_null;

    // sum_qty — decimal? (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long sum_qty_len = 0;
    result_bind[2].length = &sum_qty_len;
    result_bind[2].buffer_length = 0;
    bool sum_qty_is_null = false;
    result_bind[2].is_null = &sum_qty_is_null;

    // avg_qty — decimal? (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long avg_qty_len = 0;
    result_bind[3].length = &avg_qty_len;
    result_bind[3].buffer_length = 0;
    bool avg_qty_is_null = false;
    result_bind[3].is_null = &avg_qty_is_null;
    stmt.bind_result(result_bind);

    if (!stmt.fetch_row()) return std::nullopt;

    std::string sum_qty_val(sum_qty_len, '\0');
    result_bind[2].buffer = sum_qty_val.data();
    result_bind[2].buffer_length = sum_qty_len;
    stmt.fetch_column(&result_bind[2], 2);

    std::string avg_qty_val(avg_qty_len, '\0');
    result_bind[3].buffer = avg_qty_val.data();
    result_bind[3].buffer_length = avg_qty_len;
    stmt.fetch_column(&result_bind[3], 3);

    return GetSaleItemQuantityAggregatesRow{
        min_qty_is_null ? std::nullopt : std::optional<std::int32_t>(min_qty_val),
        max_qty_is_null ? std::nullopt : std::optional<std::int32_t>(max_qty_val),
        sum_qty_is_null ? std::nullopt : std::optional<std::string>(std::move(sum_qty_val)),
        avg_qty_is_null ? std::nullopt : std::optional<std::string>(std::move(avg_qty_val))
    };
}

std::optional<GetBookPriceAggregatesRow> get_book_price_aggregates(MYSQL* db) {
    MysqlStmt stmt(db, SQL_GET_BOOK_PRICE_AGGREGATES);
    stmt.execute();

    MYSQL_BIND result_bind[4];
    memset(result_bind, 0, sizeof(result_bind));

    // min_price — decimal? (two-phase)
    result_bind[0].buffer = nullptr; // filled below
    result_bind[0].buffer_type = MYSQL_TYPE_STRING;
    unsigned long min_price_len = 0;
    result_bind[0].length = &min_price_len;
    result_bind[0].buffer_length = 0;
    bool min_price_is_null = false;
    result_bind[0].is_null = &min_price_is_null;

    // max_price — decimal? (two-phase)
    result_bind[1].buffer = nullptr; // filled below
    result_bind[1].buffer_type = MYSQL_TYPE_STRING;
    unsigned long max_price_len = 0;
    result_bind[1].length = &max_price_len;
    result_bind[1].buffer_length = 0;
    bool max_price_is_null = false;
    result_bind[1].is_null = &max_price_is_null;

    // sum_price — decimal? (two-phase)
    result_bind[2].buffer = nullptr; // filled below
    result_bind[2].buffer_type = MYSQL_TYPE_STRING;
    unsigned long sum_price_len = 0;
    result_bind[2].length = &sum_price_len;
    result_bind[2].buffer_length = 0;
    bool sum_price_is_null = false;
    result_bind[2].is_null = &sum_price_is_null;

    // avg_price — decimal? (two-phase)
    result_bind[3].buffer = nullptr; // filled below
    result_bind[3].buffer_type = MYSQL_TYPE_STRING;
    unsigned long avg_price_len = 0;
    result_bind[3].length = &avg_price_len;
    result_bind[3].buffer_length = 0;
    bool avg_price_is_null = false;
    result_bind[3].is_null = &avg_price_is_null;
    stmt.bind_result(result_bind);

    if (!stmt.fetch_row()) return std::nullopt;

    std::string min_price_val(min_price_len, '\0');
    result_bind[0].buffer = min_price_val.data();
    result_bind[0].buffer_length = min_price_len;
    stmt.fetch_column(&result_bind[0], 0);

    std::string max_price_val(max_price_len, '\0');
    result_bind[1].buffer = max_price_val.data();
    result_bind[1].buffer_length = max_price_len;
    stmt.fetch_column(&result_bind[1], 1);

    std::string sum_price_val(sum_price_len, '\0');
    result_bind[2].buffer = sum_price_val.data();
    result_bind[2].buffer_length = sum_price_len;
    stmt.fetch_column(&result_bind[2], 2);

    std::string avg_price_val(avg_price_len, '\0');
    result_bind[3].buffer = avg_price_val.data();
    result_bind[3].buffer_length = avg_price_len;
    stmt.fetch_column(&result_bind[3], 3);

    return GetBookPriceAggregatesRow{
        min_price_is_null ? std::nullopt : std::optional<std::string>(std::move(min_price_val)),
        max_price_is_null ? std::nullopt : std::optional<std::string>(std::move(max_price_val)),
        sum_price_is_null ? std::nullopt : std::optional<std::string>(std::move(sum_price_val)),
        avg_price_is_null ? std::nullopt : std::optional<std::string>(std::move(avg_price_val))
    };
}


void Querier::create_author(const std::string& name, const std::optional<std::string>& bio, const std::optional<std::int32_t>& birth_year) {
    return ::db::create_author(db_, name, bio, birth_year);
}

std::optional<Author> Querier::get_author(std::int64_t id) {
    return ::db::get_author(db_, id);
}

std::vector<Author> Querier::list_authors() {
    return ::db::list_authors(db_);
}

void Querier::update_author_bio(const std::optional<std::string>& bio, std::int64_t id) {
    return ::db::update_author_bio(db_, bio, id);
}

void Querier::delete_author(std::int64_t id) {
    return ::db::delete_author(db_, id);
}

void Querier::create_book(std::int64_t author_id, const std::string& title, const std::string& genre, const std::string& price, const std::optional<std::string>& published_at) {
    return ::db::create_book(db_, author_id, title, genre, price, published_at);
}

std::optional<Book> Querier::get_book(std::int64_t id) {
    return ::db::get_book(db_, id);
}

std::vector<Book> Querier::get_books_by_ids(const std::vector<std::int64_t>& ids) {
    return ::db::get_books_by_ids(db_, ids);
}

std::vector<Book> Querier::list_books_by_genre(const std::string& genre) {
    return ::db::list_books_by_genre(db_, genre);
}

std::vector<Book> Querier::list_books_by_genre_or_all(const std::string& genre) {
    return ::db::list_books_by_genre_or_all(db_, genre);
}

void Querier::create_customer(const std::string& name, const std::string& email) {
    return ::db::create_customer(db_, name, email);
}

void Querier::create_sale(std::int64_t customer_id) {
    return ::db::create_sale(db_, customer_id);
}

void Querier::add_sale_item(std::int64_t sale_id, std::int64_t book_id, std::int32_t quantity, const std::string& unit_price) {
    return ::db::add_sale_item(db_, sale_id, book_id, quantity, unit_price);
}

std::vector<ListBooksWithAuthorRow> Querier::list_books_with_author() {
    return ::db::list_books_with_author(db_);
}

std::vector<Book> Querier::get_books_never_ordered() {
    return ::db::get_books_never_ordered(db_);
}

std::vector<GetTopSellingBooksRow> Querier::get_top_selling_books() {
    return ::db::get_top_selling_books(db_);
}

std::vector<GetBestCustomersRow> Querier::get_best_customers() {
    return ::db::get_best_customers(db_);
}

std::vector<CountBooksByGenreRow> Querier::count_books_by_genre() {
    return ::db::count_books_by_genre(db_);
}

std::vector<ListBooksWithLimitRow> Querier::list_books_with_limit(std::int64_t limit, std::int64_t offset) {
    return ::db::list_books_with_limit(db_, limit, offset);
}

std::vector<SearchBooksByTitleRow> Querier::search_books_by_title(const std::string& title) {
    return ::db::search_books_by_title(db_, title);
}

std::vector<GetBooksByPriceRangeRow> Querier::get_books_by_price_range(const std::string& price, const std::string& price_2) {
    return ::db::get_books_by_price_range(db_, price, price_2);
}

std::vector<GetBooksInGenresRow> Querier::get_books_in_genres(const std::string& genre, const std::string& genre_2, const std::string& genre_3) {
    return ::db::get_books_in_genres(db_, genre, genre_2, genre_3);
}

std::vector<GetBookPriceLabelRow> Querier::get_book_price_label(const std::string& price) {
    return ::db::get_book_price_label(db_, price);
}

std::vector<GetBookPriceOrDefaultRow> Querier::get_book_price_or_default(const std::optional<std::string>& price) {
    return ::db::get_book_price_or_default(db_, price);
}

std::int64_t Querier::delete_book_by_id(std::int64_t id) {
    return ::db::delete_book_by_id(db_, id);
}

std::vector<GetGenresWithManyBooksRow> Querier::get_genres_with_many_books(std::int64_t count) {
    return ::db::get_genres_with_many_books(db_, count);
}

std::vector<GetBooksByAuthorParamRow> Querier::get_books_by_author_param(const std::optional<std::int32_t>& birth_year) {
    return ::db::get_books_by_author_param(db_, birth_year);
}

std::vector<Book> Querier::get_all_book_fields() {
    return ::db::get_all_book_fields(db_);
}

std::vector<GetBooksNotByAuthorRow> Querier::get_books_not_by_author(const std::string& name) {
    return ::db::get_books_not_by_author(db_, name);
}

std::vector<GetBooksWithRecentSalesRow> Querier::get_books_with_recent_sales(const std::string& ordered_at) {
    return ::db::get_books_with_recent_sales(db_, ordered_at);
}

std::vector<GetBookWithAuthorNameRow> Querier::get_book_with_author_name() {
    return ::db::get_book_with_author_name(db_);
}

std::vector<GetAuthorStatsRow> Querier::get_author_stats() {
    return ::db::get_author_stats(db_);
}

std::optional<Product> Querier::get_product(const std::string& id) {
    return ::db::get_product(db_, id);
}

std::vector<ListActiveProductsRow> Querier::list_active_products(bool active) {
    return ::db::list_active_products(db_, active);
}

std::vector<GetAuthorsWithNullBioRow> Querier::get_authors_with_null_bio() {
    return ::db::get_authors_with_null_bio(db_);
}

std::vector<Author> Querier::get_authors_with_bio() {
    return ::db::get_authors_with_bio(db_);
}

std::vector<GetBooksPublishedBetweenRow> Querier::get_books_published_between(const std::optional<std::string>& published_at, const std::optional<std::string>& published_at_2) {
    return ::db::get_books_published_between(db_, published_at, published_at_2);
}

std::vector<GetDistinctGenresRow> Querier::get_distinct_genres() {
    return ::db::get_distinct_genres(db_);
}

std::vector<GetBooksWithSalesCountRow> Querier::get_books_with_sales_count() {
    return ::db::get_books_with_sales_count(db_);
}

std::optional<CountSaleItemsRow> Querier::count_sale_items(std::int64_t sale_id) {
    return ::db::count_sale_items(db_, sale_id);
}

std::optional<GetSaleItemQuantityAggregatesRow> Querier::get_sale_item_quantity_aggregates() {
    return ::db::get_sale_item_quantity_aggregates(db_);
}

std::optional<GetBookPriceAggregatesRow> Querier::get_book_price_aggregates() {
    return ::db::get_book_price_aggregates(db_);
}

} // namespace db
