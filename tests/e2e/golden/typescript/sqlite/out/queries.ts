// Generated by sqltgen. Do not edit.
// Runtime: better-sqlite3 — npm install better-sqlite3

import type { Database } from 'better-sqlite3';

import type { Author } from './author';
import type { Book } from './book';
import type { Product } from './product';

const SQL_CREATE_AUTHOR = "INSERT INTO author (name, bio, birth_year) VALUES (?, ?, ?)";
const SQL_GET_AUTHOR = "SELECT id, name, bio, birth_year FROM author WHERE id = ?";
const SQL_LIST_AUTHORS = "SELECT id, name, bio, birth_year FROM author ORDER BY name";
const SQL_CREATE_BOOK = "INSERT INTO book (author_id, title, genre, price, published_at) VALUES (?, ?, ?, ?, ?)";
const SQL_GET_BOOK = "SELECT id, author_id, title, genre, price, published_at FROM book WHERE id = ?";
const SQL_GET_BOOKS_BY_IDS = "SELECT id, author_id, title, genre, price, published_at FROM book WHERE id IN (SELECT value FROM json_each(?)) ORDER BY title";
const SQL_LIST_BOOKS_BY_GENRE = "SELECT id, author_id, title, genre, price, published_at FROM book WHERE genre = ? ORDER BY title";
const SQL_LIST_BOOKS_BY_GENRE_OR_ALL = "SELECT id, author_id, title, genre, price, published_at FROM book WHERE ? = 'all' OR genre = ? ORDER BY title";
const SQL_CREATE_CUSTOMER = "INSERT INTO customer (name, email) VALUES (?, ?)";
const SQL_CREATE_SALE = "INSERT INTO sale (customer_id) VALUES (?)";
const SQL_ADD_SALE_ITEM = "INSERT INTO sale_item (sale_id, book_id, quantity, unit_price) VALUES (?, ?, ?, ?)";
const SQL_LIST_BOOKS_WITH_AUTHOR = "SELECT b.id, b.title, b.genre, b.price, b.published_at,        a.name AS author_name, a.bio AS author_bio FROM book b JOIN author a ON a.id = b.author_id ORDER BY b.title";
const SQL_GET_BOOKS_NEVER_ORDERED = "SELECT b.id, b.author_id, b.title, b.genre, b.price, b.published_at FROM book b LEFT JOIN sale_item si ON si.book_id = b.id WHERE si.id IS NULL ORDER BY b.title";
const SQL_GET_TOP_SELLING_BOOKS = "WITH book_sales AS (     SELECT book_id,            SUM(quantity) AS units_sold     FROM sale_item     GROUP BY book_id ) SELECT b.id, b.title, b.genre, b.price,        bs.units_sold FROM book b JOIN book_sales bs ON bs.book_id = b.id ORDER BY bs.units_sold DESC";
const SQL_GET_BEST_CUSTOMERS = "WITH customer_spend AS (     SELECT s.customer_id,            SUM(si.quantity * si.unit_price) AS total_spent     FROM sale s     JOIN sale_item si ON si.sale_id = s.id     GROUP BY s.customer_id ) SELECT c.id, c.name, c.email,        cs.total_spent FROM customer c JOIN customer_spend cs ON cs.customer_id = c.id ORDER BY cs.total_spent DESC";
const SQL_COUNT_BOOKS_BY_GENRE = "SELECT genre, COUNT(*) AS book_count FROM book GROUP BY genre ORDER BY genre";
const SQL_LIST_BOOKS_WITH_LIMIT = "SELECT id, title, genre, price FROM book ORDER BY title LIMIT ? OFFSET ?";
const SQL_SEARCH_BOOKS_BY_TITLE = "SELECT id, title, genre, price FROM book WHERE title LIKE ? ORDER BY title";
const SQL_GET_BOOKS_BY_PRICE_RANGE = "SELECT id, title, genre, price FROM book WHERE price BETWEEN ? AND ? ORDER BY price";
const SQL_GET_BOOKS_IN_GENRES = "SELECT id, title, genre, price FROM book WHERE genre IN (?, ?, ?) ORDER BY title";
const SQL_GET_BOOK_PRICE_LABEL = "SELECT id, title, price,        CASE WHEN price > ? THEN 'expensive' ELSE 'affordable' END AS price_label FROM book ORDER BY title";
const SQL_GET_BOOK_PRICE_OR_DEFAULT = "SELECT id, title, COALESCE(price, ?) AS effective_price FROM book ORDER BY title";
const SQL_DELETE_BOOK_BY_ID = "DELETE FROM book WHERE id = ?";
const SQL_GET_GENRES_WITH_MANY_BOOKS = "SELECT genre, COUNT(*) AS book_count FROM book GROUP BY genre HAVING COUNT(*) > ? ORDER BY genre";
const SQL_GET_BOOKS_BY_AUTHOR_PARAM = "SELECT b.id, b.title, b.price FROM book b JOIN author a ON a.id = b.author_id AND a.birth_year > ? ORDER BY b.title";
const SQL_GET_ALL_BOOK_FIELDS = "SELECT b.* FROM book b ORDER BY b.id";
const SQL_GET_BOOKS_NOT_BY_AUTHOR = "SELECT id, title, genre FROM book WHERE author_id NOT IN (SELECT id FROM author WHERE name = ?) ORDER BY title";
const SQL_GET_BOOKS_WITH_RECENT_SALES = "SELECT id, title, genre FROM book WHERE EXISTS (     SELECT 1 FROM sale_item si     JOIN sale s ON s.id = si.sale_id     WHERE si.book_id = book.id AND s.ordered_at > ? ) ORDER BY title";
const SQL_GET_BOOK_WITH_AUTHOR_NAME = "SELECT b.id, b.title,        (SELECT a.name FROM author a WHERE a.id = b.author_id) AS author_name FROM book b ORDER BY b.title";
const SQL_GET_AUTHOR_STATS = "WITH book_counts AS (     SELECT author_id, COUNT(*) AS num_books     FROM book     GROUP BY author_id ), sale_counts AS (     SELECT b.author_id, SUM(si.quantity) AS total_sold     FROM sale_item si     JOIN book b ON b.id = si.book_id     GROUP BY b.author_id ) SELECT a.id, a.name,        COALESCE(bc.num_books, 0) AS num_books,        COALESCE(sc.total_sold, 0) AS total_sold FROM author a LEFT JOIN book_counts bc ON bc.author_id = a.id LEFT JOIN sale_counts sc ON sc.author_id = a.id ORDER BY a.name";
const SQL_GET_PRODUCT = "SELECT id, sku, name, active, weight_kg, rating, metadata,        thumbnail, created_at, stock_count FROM product WHERE id = ?";
const SQL_LIST_ACTIVE_PRODUCTS = "SELECT id, sku, name, active, weight_kg, rating, metadata,        created_at, stock_count FROM product WHERE active = ? ORDER BY name";
const SQL_GET_AUTHORS_WITH_NULL_BIO = "SELECT id, name, birth_year FROM author WHERE bio IS NULL ORDER BY name";
const SQL_GET_AUTHORS_WITH_BIO = "SELECT id, name, bio, birth_year FROM author WHERE bio IS NOT NULL ORDER BY name";
const SQL_GET_BOOKS_PUBLISHED_BETWEEN = "SELECT id, title, genre, price, published_at FROM book WHERE published_at IS NOT NULL   AND published_at BETWEEN ? AND ? ORDER BY published_at";
const SQL_GET_DISTINCT_GENRES = "SELECT DISTINCT genre FROM book ORDER BY genre";
const SQL_GET_BOOKS_WITH_SALES_COUNT = "SELECT b.id, b.title, b.genre,        COALESCE(SUM(si.quantity), 0) AS total_quantity FROM book b LEFT JOIN sale_item si ON si.book_id = b.id GROUP BY b.id, b.title, b.genre ORDER BY total_quantity DESC, b.title";
const SQL_COUNT_SALE_ITEMS = "SELECT COUNT(*) AS item_count FROM sale_item WHERE sale_id = ?";

export async function createAuthor(db: Database, name: string, bio: string | null, birthYear: number | null): Promise<void> {
  db.prepare(SQL_CREATE_AUTHOR).run(name, bio, birthYear);
}

export async function getAuthor(db: Database, id: number): Promise<Author | null> {
  const row = db.prepare(SQL_GET_AUTHOR).get(id) as Author | undefined;
  return row ?? null;
}

export async function listAuthors(db: Database): Promise<Author[]> {
  return db.prepare(SQL_LIST_AUTHORS).all() as Author[];
}

export async function createBook(db: Database, authorId: number, title: string, genre: string, price: number, publishedAt: string | null): Promise<void> {
  db.prepare(SQL_CREATE_BOOK).run(authorId, title, genre, price, publishedAt);
}

export async function getBook(db: Database, id: number): Promise<Book | null> {
  const row = db.prepare(SQL_GET_BOOK).get(id) as Book | undefined;
  return row ?? null;
}

export async function getBooksByIds(db: Database, ids: number[]): Promise<Book[]> {
  const idsJson = JSON.stringify(ids);
  return db.prepare(SQL_GET_BOOKS_BY_IDS).all(idsJson) as Book[];
}

export async function listBooksByGenre(db: Database, genre: string): Promise<Book[]> {
  return db.prepare(SQL_LIST_BOOKS_BY_GENRE).all(genre) as Book[];
}

export async function listBooksByGenreOrAll(db: Database, genre: string): Promise<Book[]> {
  return db.prepare(SQL_LIST_BOOKS_BY_GENRE_OR_ALL).all(genre, genre) as Book[];
}

export async function createCustomer(db: Database, name: string, email: string): Promise<void> {
  db.prepare(SQL_CREATE_CUSTOMER).run(name, email);
}

export async function createSale(db: Database, customerId: number): Promise<void> {
  db.prepare(SQL_CREATE_SALE).run(customerId);
}

export async function addSaleItem(db: Database, saleId: number, bookId: number, quantity: number, unitPrice: number): Promise<void> {
  db.prepare(SQL_ADD_SALE_ITEM).run(saleId, bookId, quantity, unitPrice);
}

export interface ListBooksWithAuthorRow {
  id: number;
  title: string;
  genre: string;
  price: number;
  published_at: string | null;
  author_name: string;
  author_bio: string | null;
}

export async function listBooksWithAuthor(db: Database): Promise<ListBooksWithAuthorRow[]> {
  return db.prepare(SQL_LIST_BOOKS_WITH_AUTHOR).all() as ListBooksWithAuthorRow[];
}

export async function getBooksNeverOrdered(db: Database): Promise<Book[]> {
  return db.prepare(SQL_GET_BOOKS_NEVER_ORDERED).all() as Book[];
}

export interface GetTopSellingBooksRow {
  id: number;
  title: string;
  genre: string;
  price: number;
  units_sold: number | null;
}

export async function getTopSellingBooks(db: Database): Promise<GetTopSellingBooksRow[]> {
  return db.prepare(SQL_GET_TOP_SELLING_BOOKS).all() as GetTopSellingBooksRow[];
}

export interface GetBestCustomersRow {
  id: number;
  name: string;
  email: string;
  total_spent: number | null;
}

export async function getBestCustomers(db: Database): Promise<GetBestCustomersRow[]> {
  return db.prepare(SQL_GET_BEST_CUSTOMERS).all() as GetBestCustomersRow[];
}

export interface CountBooksByGenreRow {
  genre: string;
  book_count: number;
}

export async function countBooksByGenre(db: Database): Promise<CountBooksByGenreRow[]> {
  return db.prepare(SQL_COUNT_BOOKS_BY_GENRE).all() as CountBooksByGenreRow[];
}

export interface ListBooksWithLimitRow {
  id: number;
  title: string;
  genre: string;
  price: number;
}

export async function listBooksWithLimit(db: Database, limit: number, offset: number): Promise<ListBooksWithLimitRow[]> {
  return db.prepare(SQL_LIST_BOOKS_WITH_LIMIT).all(limit, offset) as ListBooksWithLimitRow[];
}

export interface SearchBooksByTitleRow {
  id: number;
  title: string;
  genre: string;
  price: number;
}

export async function searchBooksByTitle(db: Database, title: string): Promise<SearchBooksByTitleRow[]> {
  return db.prepare(SQL_SEARCH_BOOKS_BY_TITLE).all(title) as SearchBooksByTitleRow[];
}

export interface GetBooksByPriceRangeRow {
  id: number;
  title: string;
  genre: string;
  price: number;
}

export async function getBooksByPriceRange(db: Database, price: number, price2: number): Promise<GetBooksByPriceRangeRow[]> {
  return db.prepare(SQL_GET_BOOKS_BY_PRICE_RANGE).all(price, price2) as GetBooksByPriceRangeRow[];
}

export interface GetBooksInGenresRow {
  id: number;
  title: string;
  genre: string;
  price: number;
}

export async function getBooksInGenres(db: Database, genre: string, genre2: string, genre3: string): Promise<GetBooksInGenresRow[]> {
  return db.prepare(SQL_GET_BOOKS_IN_GENRES).all(genre, genre2, genre3) as GetBooksInGenresRow[];
}

export interface GetBookPriceLabelRow {
  id: number;
  title: string;
  price: number;
  price_label: string;
}

export async function getBookPriceLabel(db: Database, price: number): Promise<GetBookPriceLabelRow[]> {
  return db.prepare(SQL_GET_BOOK_PRICE_LABEL).all(price) as GetBookPriceLabelRow[];
}

export interface GetBookPriceOrDefaultRow {
  id: number;
  title: string;
  effective_price: number;
}

export async function getBookPriceOrDefault(db: Database, price: number | null): Promise<GetBookPriceOrDefaultRow[]> {
  return db.prepare(SQL_GET_BOOK_PRICE_OR_DEFAULT).all(price) as GetBookPriceOrDefaultRow[];
}

export async function deleteBookById(db: Database, id: number): Promise<number> {
  const result = db.prepare(SQL_DELETE_BOOK_BY_ID).run(id);
  return result.changes;
}

export interface GetGenresWithManyBooksRow {
  genre: string;
  book_count: number;
}

export async function getGenresWithManyBooks(db: Database, count: number): Promise<GetGenresWithManyBooksRow[]> {
  return db.prepare(SQL_GET_GENRES_WITH_MANY_BOOKS).all(count) as GetGenresWithManyBooksRow[];
}

export interface GetBooksByAuthorParamRow {
  id: number;
  title: string;
  price: number;
}

export async function getBooksByAuthorParam(db: Database, birthYear: number | null): Promise<GetBooksByAuthorParamRow[]> {
  return db.prepare(SQL_GET_BOOKS_BY_AUTHOR_PARAM).all(birthYear) as GetBooksByAuthorParamRow[];
}

export async function getAllBookFields(db: Database): Promise<Book[]> {
  return db.prepare(SQL_GET_ALL_BOOK_FIELDS).all() as Book[];
}

export interface GetBooksNotByAuthorRow {
  id: number;
  title: string;
  genre: string;
}

export async function getBooksNotByAuthor(db: Database, name: string): Promise<GetBooksNotByAuthorRow[]> {
  return db.prepare(SQL_GET_BOOKS_NOT_BY_AUTHOR).all(name) as GetBooksNotByAuthorRow[];
}

export interface GetBooksWithRecentSalesRow {
  id: number;
  title: string;
  genre: string;
}

export async function getBooksWithRecentSales(db: Database, orderedAt: unknown): Promise<GetBooksWithRecentSalesRow[]> {
  return db.prepare(SQL_GET_BOOKS_WITH_RECENT_SALES).all(orderedAt) as GetBooksWithRecentSalesRow[];
}

export interface GetBookWithAuthorNameRow {
  id: number;
  title: string;
  author_name: unknown | null;
}

export async function getBookWithAuthorName(db: Database): Promise<GetBookWithAuthorNameRow[]> {
  return db.prepare(SQL_GET_BOOK_WITH_AUTHOR_NAME).all() as GetBookWithAuthorNameRow[];
}

export interface GetAuthorStatsRow {
  id: number;
  name: string;
  num_books: number;
  total_sold: number;
}

export async function getAuthorStats(db: Database): Promise<GetAuthorStatsRow[]> {
  return db.prepare(SQL_GET_AUTHOR_STATS).all() as GetAuthorStatsRow[];
}

export async function getProduct(db: Database, id: string): Promise<Product | null> {
  const row = db.prepare(SQL_GET_PRODUCT).get(id) as Product | undefined;
  return row ?? null;
}

export interface ListActiveProductsRow {
  id: string;
  sku: string;
  name: string;
  active: number;
  weight_kg: number | null;
  rating: number | null;
  metadata: string | null;
  created_at: string;
  stock_count: number;
}

export async function listActiveProducts(db: Database, active: number): Promise<ListActiveProductsRow[]> {
  return db.prepare(SQL_LIST_ACTIVE_PRODUCTS).all(active) as ListActiveProductsRow[];
}

export interface GetAuthorsWithNullBioRow {
  id: number;
  name: string;
  birth_year: number | null;
}

export async function getAuthorsWithNullBio(db: Database): Promise<GetAuthorsWithNullBioRow[]> {
  return db.prepare(SQL_GET_AUTHORS_WITH_NULL_BIO).all() as GetAuthorsWithNullBioRow[];
}

export async function getAuthorsWithBio(db: Database): Promise<Author[]> {
  return db.prepare(SQL_GET_AUTHORS_WITH_BIO).all() as Author[];
}

export interface GetBooksPublishedBetweenRow {
  id: number;
  title: string;
  genre: string;
  price: number;
  published_at: string | null;
}

export async function getBooksPublishedBetween(db: Database, publishedAt: string | null, publishedAt2: string | null): Promise<GetBooksPublishedBetweenRow[]> {
  return db.prepare(SQL_GET_BOOKS_PUBLISHED_BETWEEN).all(publishedAt, publishedAt2) as GetBooksPublishedBetweenRow[];
}

export interface GetDistinctGenresRow {
  genre: string;
}

export async function getDistinctGenres(db: Database): Promise<GetDistinctGenresRow[]> {
  return db.prepare(SQL_GET_DISTINCT_GENRES).all() as GetDistinctGenresRow[];
}

export interface GetBooksWithSalesCountRow {
  id: number;
  title: string;
  genre: string;
  total_quantity: number;
}

export async function getBooksWithSalesCount(db: Database): Promise<GetBooksWithSalesCountRow[]> {
  return db.prepare(SQL_GET_BOOKS_WITH_SALES_COUNT).all() as GetBooksWithSalesCountRow[];
}

export interface CountSaleItemsRow {
  item_count: number;
}

export async function countSaleItems(db: Database, saleId: number): Promise<CountSaleItemsRow | null> {
  const row = db.prepare(SQL_COUNT_SALE_ITEMS).get(saleId) as CountSaleItemsRow | undefined;
  return row ?? null;
}
